# Apeireth v1.0.0 — Release Notes（正式版）

> **Tag**: `v1.0.0`（2026-08-18 主人拍板：后端收工，本版本才是**真正的 1.0**）
> **定位**: 从"代码演进号 1.2.0 / 产品轴 v2.0.0-alpha"统一收口为 **1.0.0 正式版**
> **基线**: master = integration（0/0 同步，commit `b7132fad`）
> **验证**: `cargo test --workspace` 全绿（368 组，含真实 API 压测 100/100）+ `--all-targets` 编译干净 + 真实 LLM 端到端实测通过

---

## 🎉 Highlights

**Apeireth 1.0.0 = 后端机制层正式收工版**。这一版不是"能跑"，是**她真的在跑**：

- **五原型全部有骨架**：世界模型（W1 文本模拟器 + W2/W3 因果结构图，发布前置完成）、自主好奇心（E4 记忆回声偏置）、假设检验闭环（F4，主人钦定"有了它她才能闭环想法进步"）、连续感知地基（A4 统一事件桥 + PerceptionGate）、价值内化（F6 案例库+反馈回流）
- **她本身**：情感记忆（F1 主人情绪时间线，mood 接线进运行时）、开口策略（E7 涌现循环）、反思/做梦/提炼全自动、渐进式披露注入（TP21 目录+按需展开）
- **真实 LLM 端到端跑通**：companion_serve（真 MiniMax-M3）实测——人格化对话 + 记忆注入 + 工具执行 + 权限洋葱审批流
- **安全底线**：双洋葱 + S4 出站白名单（默认拒绝 + SHA-256 审计链）+ Windows Job Object 沙箱 + ApprovalBridge（silent 透传）
- **验收干净**：TP11-TP30 全批次树实测（3 项报告夸大已补实现），全量 368 组测试 0 失败

## 📦 工程事实（实测）

| 项 | 值 |
|---|---|
| active crate | 84（三层生态：模块/套件/插件） |
| 代码量 | ~34 万行 Rust |
| 测试 | 全量 368 组 0 失败（含真实 API 压测退避后 100/100） |
| 编译 | workspace --all-targets 干净 |
| 仓库 | 历史净化后 .git 356MB（GitHub 友好），0 大 blob |
| License | Apache-2.0 |

## ✅ 发布前置（全部完成）

- 世界模型前两层（W1 文本模拟器 + W2/W3 因果推演）——主人拍板"发布前完成"项
- 工程门槛：compose 密码外部化（`${VAR:?err}` 强制注入 + .env.example）、.gitignore 密钥加固（五通配）、cosign 公钥、cargo vet + SBOM 链（TP20-S5）
- 历史净化：误产物清除（.scratch_n4/日志）、1.5GB+ 历史大 blob 剥离、90 个历史分支清理

## ⚠️ 诚实标注（0 装 PASS）

| 项 | 状态 |
|---|---|
| Docker 构建实测 | 本机无 docker，**待实测**（Dockerfile 已修复，诚实标注） |
| LLM 接入层 | E4 探索行为 / F4 提问生成 / TP25 TimesFM-Kronos 模型——trait 口已备，实现者未接（她长大时该长） |
| 产品形态 | 桌宠 / Tauri / 麦克风实时语音 / 屏幕显著性事件——08 愿景文档规划中，非本版范围 |
| 投资模拟盘主链 | TP25-27 零件已备（时序/事件/标的），模拟盘本体未做 |

## 🔮 下一站

产品形态（桌宠/语音）→ 投资场景主链（模拟盘）→ 正式发布动作（docker 实测 + GitHub Release）。

---

*主人 2026-08-18：*「我们现在这个版本才是真正的 1.0 版本」
