# R122-8 stage — 多语言 SDK skeleton 实施

**时间**: 2026-08-10 14:25 (readmap 完)
**状态**: 进入实施
**节奏**: 8 min readmap ✓ → 30 min 实施 (14:25-14:55) → 17 min verify + report (14:55-15:12)

---

## 实施序列

### Step 1: Cargo.toml 改 (3 min, 14:25-14:28)

**位置**: `crates/apeireth-sdk/Cargo.toml`

**改动**:
- 加 `[features]` 段 (default = [], python/node/c opt-in)
- 加 4 optional deps: pyo3 (workspace 0.29, extension-module), napi (2), napi-derive (2), sha2 (0.10)
- 加 1 build-dep: cbindgen (0.26)
- 0 改 description / version / 现有 dependencies / dev-dependencies / lints

### Step 2: lib.rs 改 (2 min, 14:28-14:30)

**位置**: `crates/apeireth-sdk/src/lib.rs`

**改动**:
- 在 mod 声明区域 (line 257-262) 加 3 行 cfg-gated:
  ```rust
  #[cfg(feature = "python")]
  pub mod python;
  #[cfg(feature = "node")]
  pub mod node;
  #[cfg(feature = "c")]
  pub mod c;
  ```
- 在 O-5 哲学锚段 (line 52-59) 末尾加 1 段 R122-8 注释 (5-10 行, 0 改 6 哲学锚定义)

### Step 3: python.rs 新建 (5 min, 14:30-14:35)

**位置**: `crates/apeireth-sdk/src/python.rs` (~100 行)

**内容**:
- `#![cfg(feature = "python")]` + `#![allow(unsafe_code)]` (pyo3 macro 需要)
- `#[pyo3::pymodule] pub fn apeireth_sdk_py(_py: Python, m: &Bound<PyModule>) -> PyResult<()>`
- 1 fn: `py_count_tokens(text: &str, model: &str) -> PyResult<usize>` (inline 简版 R32-1 算法, 0 dep)
- 3 unit tests: ascii_only, cjk_only, mixed

### Step 4: node.rs 新建 (5 min, 14:35-14:40)

**位置**: `crates/apeireth-sdk/src/node.rs` (~100 行)

**内容**:
- `#![cfg(feature = "node")]` + `#![allow(unsafe_code)]` (napi 桥接需要)
- 2 fn:
  - `#[napi_derive::napi] pub fn count_tokens(text: String, model: String) -> u32`
  - `#[napi_derive::napi] pub fn hash_request(method: String, url: String, body: Buffer) -> String` (SHA-256 hex)
- 3 unit tests: count_tokens ascii, count_tokens cjk, hash_request deterministic

### Step 5: c.rs + build.rs + apeireth_sdk.h 新建 (7 min, 14:40-14:47)

**位置**:
- `crates/apeireth-sdk/src/c.rs` (~150 行)
- `crates/apeireth-sdk/build.rs` (~25 行)
- `crates/apeireth-sdk/apeireth_sdk.h` (cbindgen 自动生成)

**内容**:
- `c.rs` 5 fn C 签名:
  - `count_tokens_c(text: *const c_char) -> u32`
  - `hash_request_c(method: *const c_char, url: *const c_char, body: *const u8, body_len: usize) -> *mut c_char` (返 *mut c_char 由 free_string_c 释放)
  - `version_c() -> *const c_char` (返 "1.1.0" 字面量, 0 改 workspace.version)
  - `compile_info_c() -> *const c_char` (返 "rustc X.Y.Z" 编译信息)
  - `free_string_c(ptr: *mut c_char)` (释放 C string)
- 3 unit tests: count_tokens 1, hash_request 2 (deterministic + different)
- `build.rs` 调 cbindgen, 仅 feature c 启用时 generate header
- `apeireth_sdk.h` cbindgen 自动生成 (5 fn C extern 声明)

### Step 6: multilang_ffi.rs 集成测试 (5 min, 14:47-14:52)

**位置**: `crates/apeireth-sdk/tests/multilang_ffi.rs` (~150 行)

**5 集成 test** (cfg feature gated):
1. `sdk_python_ffi_count_tokens_works` (cfg feature python)
2. `sdk_node_ffi_count_tokens_works` (cfg feature node)
3. `sdk_c_ffi_hash_request_returns_same_value_as_rust` (cfg feature c)
4. `sdk_c_ffi_version_returns_semver` (cfg feature c)
5. `sdk_compile_info_includes_features` (cfg always, 测试 compile_info_c 输出含 "apeireth-sdk" + "python/node/c 之一")

### Step 7: c_consumer_demo.c 示例 (2 min, 14:52-14:54)

**位置**: `crates/apeireth-sdk/examples/c_consumer_demo.c` (~50 行)

**内容**:
- `#include "apeireth_sdk.h"`
- main() 调 5 fn: version_c (print), compile_info_c (print), count_tokens_c (print "Hello, world!" token count), hash_request_c (print SHA-256 hex of POST /v1/tools/web_search/invoke + body "{}"), free_string_c (清理)

### Step 8: Verify (10 min, 14:55-15:05)

- `cargo build -p apeireth-sdk` (default features, 0 error)
- `cargo build -p apeireth-sdk --features python` (0 error)
- `cargo build -p apeireth-sdk --features node` (0 error)
- `cargo build -p apeireth-sdk --features c` (0 error, 触发 cbindgen build.rs)
- `cargo test -p apeireth-sdk --lib` (5+ passed, 0 failed)
- `cargo test -p apeireth-sdk --tests` (5 integration test 触发)
- `cargo test --workspace --no-run` (19972 + 5+ compile, 0 failed)

### Step 9: Final + Decision Log (10 min, 15:05-15:15)

- `reports/agent-r122-8-final-2026-08-10.md` (验收清单 + 改动清单 + 风险登记 + R123 切换路径)
- `reports/agent-r122-8-decision-log-2026-08-10.md` (3 决策 #1 cfg-gated / #2 pyo3 workspace 复用 / #3 inline 简版)

---

## 验证清单 (per task)

| 项 | 期望 | 实测 |
|---|------|------|
| `cargo build -p apeireth-sdk` | 0 error | TBD |
| `cargo build -p apeireth-sdk --features python` | 0 error | TBD |
| `cargo build -p apeireth-sdk --features node` | 0 error | TBD |
| `cargo build -p apeireth-sdk --features c` | 0 error | TBD |
| `cargo test -p apeireth-sdk --lib` | 5+ passed, 0 failed | TBD |
| `cargo test --workspace` | 19972+ passed, 0 failed | TBD |
| 11 agent 公共 API 签名 | 0 改 | TBD |
| 24 LOCKED mtime | 0 触碰 | TBD |
| workspace.version 1.1.0 | 0 改 | TBD |

**Stage 完, 开始实施. 14:25 启动 step 1.**
