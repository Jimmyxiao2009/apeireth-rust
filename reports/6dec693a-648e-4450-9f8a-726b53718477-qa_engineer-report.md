# 自审报告 — QA Engineer 1 · C1 主链路 e2e 场景测试

- **任务**: 6dec693a-648e-4450-9f8a-726b53718477 (team-work-doc §4 C1 系统性欠账: 跨 crate 主链路 e2e 覆盖为 0)
- **角色**: qa_engineer (e2e 场景测试)
- **日期**: 2026-08-17
- **提交**: 3 笔 (基线版全绿 → 台账登记 → 最终状态锁定), 分支 master

## 1. 交付物

| 文件 | 状态 | 说明 |
|---|---|---|
| `crates/apeireth-integration-e2e/tests/test_main_chain_e2e.rs` | ✅ 激活, 10/10 全绿 | 基线版 10 场景 (不依赖 companion, 用 core/memory/context-fold/tool-* 真机制) |
| `crates/apeireth-integration-e2e/tests/pending_companion_wip/test_main_chain_e2e_full.rs` | ⏸ 暂存 (companion WIP 震荡) | 完整版 10 场景 (companion 真件: build_memory_injection / ReflectionScheduler / MultiSink); 转绿窗口内 9/10 通过, 代码就绪, 文件头含 3 步启用指引 |
| `crates/apeireth-integration-e2e/Cargo.toml` | ✅ | +apeireth-context-fold dev-dep; apeireth-companion dev-dep 暂注释 (pending) |
| `docs/backlog.md` | ✅ | +#48 (FileOperator 参数契约) +#49 (companion WIP 阻塞观察点) |

**验收命令结果** (`cargo test -p apeireth-integration-e2e -j 4`):

```
lib 单测:                     107 passed; 0 failed
test_integration_e2e_in_process: 66 passed; 0 failed (既有)
test_main_chain_e2e (C1 新增):    10 passed; 0 failed
doc-tests:                      0 passed
合计: 183 passed / 0 failed ✅
```

## 2. 10 场景清单与证据

| # | 场景 | 覆盖段 | 机制 (基线版) | 结果 |
|---|---|---|---|---|
| S1 | 记忆写入 → 次轮注入可见 | 记忆→注入 | 文件库写 3 条 → 关闭重开 → recent 检索 + context-fold::fold_segments 语义注入 (BigramOverlapScorer 确定性: 相关段保留/低相关折叠, 空记忆无注入) | ✅ |
| S2 | 会话恢复 | 记忆 | 2 session ×2 条 → drop → 重开: count/get/query/时间序/migrations 全一致 | ✅ |
| S3 | 工具调用→审批→执行→审计 | 工具 | register_all 8 真工具 → 真 parser 解析 mock LLM marker → WhitelistRule → ToolExecutor 真执行 FileOperator read (断言文件内容回显) → RecordStore 留痕 + 审批历史 | ✅ |
| S4 | 黑名单拒绝→审计留痕 (边界) | 工具 | BlacklistRule Deny → 0 执行 → record_failure 留痕可查 | ✅ |
| S5 | 反思周期触发→落盘 | 反思 | VirtualClock 快进 0 等待 → ReflectionCycleScheduler 4 阶段状态机 → reflect- episode 落盘 → **重开验证** | ✅ |
| S6 | importance 积累触发 (边界: 周期未到) | 反思 | 16 条 imp:10 (和 160>150) → 时钟 0 快进也触发 | ✅ |
| S7 | mock LLM 注入→产物落盘 | 反思 | fold_segments summarizer 闭包注入 + 深度反思文本落盘 | ✅ |
| S8 | 多 sink 送达 | 送达 | Thought/Action/Reflection 3 历史流真扇出 → 3 通道可查 → 重开仍持久 | ✅ |
| S9 | 送达部分失败隔离 (边界) | 送达 | 中间通道故障 → 前后通道均送达 + Err 如实透传 (MultiSink 契约) | ✅ |
| S10 | 五段全链路串联 + 恢复验证 | 全链路 | 记忆→注入→工具→反思→送达 单测试贯穿, 关库重开后 记忆/反思/审计/送达 全部恢复 | ✅ |

**确定性**: VirtualClock (0 sleep) / BigramOverlapScorer (确定性评分) / tempdir 文件库 / 固定时间戳. 0 网络 / 0 真实 LLM / 0 生产库污染. 全部 `#[tokio::test]` 或同步, 单文件总耗时 ~0.05s.

**完整版窗口验证**: companion 短暂转绿期间跑 `test_main_chain_e2e_full` → **9/10 通过**; 唯一失败 S8 根因是 `BroadcastSink::send` 真机制带 `[他说]` 渲染前缀 (daemon.rs:402-405), 断言已按真机制修正 — 即完整版代码已就绪, 仅缺一次 companion 稳定窗口内的全绿记录.

## 3. 边界条件覆盖 (任务要求)

- 空记忆 → 无注入 (S1)
- 低相关记忆 → 折叠不丢 (S1)
- 周期未到 → 不反思; 周期到 → 反思 (S5)
- 周期未到但 importance 积累 → 也反思 (S6)
- 白名单放行 vs 黑名单拒绝 (S3/S4)
- 拒绝后 0 执行但必审计 (S4)
- 单通道故障 → 其余通道不受影响 + 错误透传 (S9)
- 跨重启恢复 (S2/S5/S8/S10 均含关闭重开断言)

## 4. 如实申报: 装 PASS 自查 (0 装 PASS 承诺)

| 自查点 | 结论 |
|---|---|
| 是否用 `--ignored`/feature gate 隐藏失败测试? | 否. pending 目录是 cargo 不编译的普通子目录, 文件头如实标注 PENDING + 部分验证记录, 非 ignored 装死 |
| 断言是否只查"不 panic"? | 否. 全部断言具体产物 (内容/计数/轮次/前缀/持久化恢复) |
| 基线版是否冒充完整版? | 否. 文件头有降级对照表, 注入段基线用 context-fold 真机制 (非自造渲染器自测), 反思用 memory::ReflectionCycleScheduler 真件 + 对齐 companion 写回契约 |
| S6 importance 解析是镜像实现? | 是 (如实标注): 规则 1:1 镜像 companion::memory_extractor::parse_importance (因真件所在 crate 编译阻塞), 完整版用真件 |

## 5. 发现的问题 → 台账

| # | 问题 | 严重度 | 处置 |
|---|---|---|---|
| 1 | **FileOperator 参数契约是 `op` 非 `archery`**: 旧 example (`tool_orchestrator_e2e.rs`) 的调用方式执行必失败 (`missing 'op' string`), 该 example 的"executed"统计从未真成功过 | P3 | 台账 #48; e2e 已用正确契约; example 未改 (QA 边界) |
| 2 | **apeireth-companion 并行 WIP 编译震荡**: 未跟踪新文件 (N2-N8 VCP 吸收项) 先后触发 E0658/E0599/E0433/E0521/E0277/E0308/E0432, QA 窗口内红绿交替 (末态: job_object windows-sys 重构中). 阻塞一切依赖 companion 的测试编译 | 观察 | 台账 #49; 完整版已隔离 pending 不影响验收 |
| 3 | **BroadcastSink 送达带 `[他说]` 前缀**: 下游若按原文匹配会失配 — 属真机制非 bug, 完整版断言已贴合 | 信息 | 完整版注释说明 |

## 6. 与上轮 QA (08874192) 对比

- 上轮结论: "跨 crate 主链路 e2e 覆盖为 0, 无端到端场景测试" → **本轮补齐 10 场景基线版 (全绿) + 完整版 (就绪待启用)**
- 上轮 8-promises 审计仍通过 (本轮验收含 `test_workspace_8_promises_audit_passes_integration` ✅, LOCKED 文件 0 触碰)
- 未修改任何产品代码; 仅动测试 crate (Cargo.toml + tests/) 与 docs/backlog.md

## 7. 遗留/后续

1. companion WIP 编译稳定后启用完整版 (文件头 3 步), 预期 10/10 (9/10 已实测, S8 断言已修正)
2. 建议后端修 `tool_orchestrator_e2e.rs` 的 FileOperator 调用契约 (台账 #48)
3. 并行成员修改密集期, e2e crate 的 dev-dep 编译受共享工作树状态影响 — 建议 integration 分支合流前跑一次本 crate 全量验收
