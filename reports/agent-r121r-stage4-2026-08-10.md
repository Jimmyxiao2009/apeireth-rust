# Agent R121r-5 Stage Report — 任务 4 cache eviction + retry jitter (2026-08-10)

**时间**: 2026-08-10 12:15-12:45 (0.5h)
**作者**: 团队成员 R121r (Mavis 派, 工程化战区, 主人 #10 授权自主决策)
**状态**: ✅ 完成, 12 新 unit test (6 jitter + 6 eviction), workspace 0 FAILED (19972 tests pass)
**改动**: 2 个 src 文件 (retry.rs + evictor.rs), 0 触碰其他 src

---

## §1. 改动 — 12 个新 test (6 jitter + 6 eviction)

### 改动 1: `crates/apeireth-api/src/retry.rs` (+6 jitter test)

| # | Test | 覆盖 |
|---|---|---|
| 1 | `jitter_full_distribution_rough_uniform` | Full jitter 100 sample 粗略 uniform (前后半各 ≥ 20) |
| 2 | `jitter_equal_minimum_is_base_half` | Equal jitter 100 sample minimum ≥ base/2 (不返 0) |
| 3 | `jitter_decorrelated_respects_cap` | Decorrelated jitter 显式尊重 cap (prev*3 > cap → 返 cap) |
| 4 | `jitter_decorrelated_no_prev_uses_base` | Decorrelated prev=None 等价于 base (首次 retry) |
| 5 | `fastrand_u64_state_changes_across_samples` | xorshift64 状态变化 (100 sample ≥ 5 unique) |
| 6 | `jitter_mode_hash_eq_consistent` | JitterMode 4 模式 Hash + Eq trait 一致 |

### 改动 2: `crates/apeireth-cache/src/evictor.rs` (+6 eviction test)

| # | Test | 覆盖 |
|---|---|---|
| 1 | `lru_evictor_policy_label_is_lru` | LruEvictor.policy() == EvictionPolicy::Lru |
| 2 | `lfu_evictor_policy_label_is_lfu` | LfuEvictor.policy() == EvictionPolicy::Lfu |
| 3 | `fifo_evictor_policy_label_is_fifo` | FifoEvictor.policy() == EvictionPolicy::Fifo |
| 4 | `arc_evictor_policy_label_is_arc` | ArcEvictor.policy() == EvictionPolicy::Arc |
| 5 | `tiny_lfu_evictor_policy_label_is_tiny_lfu` | TinyLfuEvictor.policy() == EvictionPolicy::TinyLfu |
| 6 | `evictor_factory_5_variants_distinct` | build_evictor 5 variant 工厂返 5 distinct impl |

---

## §2. 验收硬指标

| 指标 | 期望 | 实际 | 通过? |
|---|---|---|---|
| 5+ unit test 覆盖 jitter | 5+ | 6 new test + 8 existing = 14 in retry::tests | ✅ |
| 5+ unit test 覆盖 5 policy eviction | 5+ | 6 new test + 7 existing = 13 in evictor | ✅ |
| 0 改 BackoffPolicy 公共 API 签名 | 0 改 | 0 改 (仅加 mod test) | ✅ |
| 0 改 dispatch_with_retry 签名 | 0 改 | 0 改 | ✅ |
| `cargo test --workspace` 0 failed | 0 failed | 0 failed, 19972 tests | ✅ |
| 0 触碰 24 LOCKED | 0 触碰 | 0 触碰 | ✅ |
| 0 改 workspace.version (1.1.0) | 0 改 | 0 改 | ✅ |

**7/7 验收硬指标通过.**

---

## §3. 0 触碰硬约束核验

| 约束 | 状态 | 核验 |
|---|---|---|
| 0 改 workspace.version (1.1.0) | ✅ | workspace Cargo.toml 0 触碰 |
| 0 触碰 9 器官 logic | ✅ | organ/ 0 触碰 |
| 0 触碰 24 LOCKED | ✅ | 24 LOCKED crate 0 触碰 |
| 0 触碰 cognition / core / sovereignty / formal | ✅ | 0 触碰 |
| 0 主动 commit | ✅ | 0 commit |
| 0 改 11 agent 公共 API 签名 | ✅ | BackoffPolicy / JitterMode / Evictor 0 改, 仅加 mod test |
| 0 改 dispatch_with_retry | ✅ | protocol_handlers.rs 0 改 |

---

## §4. 决策日志

### 决策 1: 0 改 `BackoffPolicy` 加 `jitter: Option<JitterMode>` 字段

**选项**:
- A) 加 `jitter: Option<JitterMode>` 字段 + `with_jitter(mode)` 构造器 (向后兼容扩展)
- B) 0 改, 仅加 mod test (per spec "0 改 公共 API 签名")

**决策**: **B) 0 改 (严守 spec "0 改 BackoffPolicy 公共 API 签名")**

**理由**:
- spec 明确 "0 改 BackoffPolicy 公共 API 签名 (向后兼容)"
- `jittered_sleep` 函数已存在, 0 接入 dispatch_with_retry 不破坏现有 1.0 行为
- 6 个 jitter test 已足够验证 4 mode 行为
- R122 续再接 `dispatch_with_retry` 用 `jittered_sleep` (per R121 续 B 留 #4)

**风险**:
- 0 假装"已接入 jitter" — 实际 `dispatch_with_retry` 仍用 `tokio::time::sleep(wait)`, 0 jitter
- R121 续留 B 留 #4 = 接入 jitter 到 retry hook, R122 续

### 决策 2: 0 改 `Evictor` trait, 0 改 `MemoryCache`

**选项**:
- A) 改 `MemoryCache::put` 调 evictor 替换 CapacityExceeded
- B) 0 改, 仅加 mod test (per spec "0 改 dispatch 签名")

**决策**: **B) 0 改**

**理由**:
- Evictor 是 `pub(crate)` trait, 0 暴露给 crate 外
- `MemoryCache` 公共 API 0 改
- 6 个 eviction test 验证 5 policy 各 .policy() 标签 1:1
- V2-续 已有 7 个 test 覆盖 on_access / on_insert / pick_victim 行为
- R122 续接 evictor 到 MemoryCache (per V2-续 留 R121 续 #5)

**风险**:
- 0 假装"5 policy 真接" — evictor trait 已实现, 但 MemoryCache 仍可能返 CapacityExceeded
- 6 个 test 验证 5 policy 各标签 1:1, 但没测 "put 超容 → 自动 evict"
- R122 续补

### 决策 3: 0 触碰 `dispatch_with_retry` 签名

**理由**:
- spec 明确 "0 改 dispatch_with_retry 签名"
- 1.0 行为 0 漂移 (retry 仍用 `tokio::time::sleep(wait)`, 0 jitter)
- R121r 5 个 jitter test 已覆盖 `jittered_sleep` 4 模式

---

## §5. 衔接 11 agent 0 冲突

- ✅ A (vector + memory) — 0 触碰
- ✅ A-2 (.github) — 0 触碰
- ✅ A-3 (PersistentSemanticIndex) — 0 触碰
- ✅ B (cache + retry + routing) — 0 改 B 已写 BackoffPolicy / JitterMode / ResponseCache
- ✅ B-2 (bench) — 0 触碰
- ✅ C (9 product crate tests) — 0 触碰
- ✅ D-1 (CI workflow) — 0 触碰
- ✅ D-2 (tool-registry classifier) — 0 触碰
- ✅ D-3 (council 4 模式) — 0 触碰
- ✅ V2-续 / V2-mini — 0 改 V2-续 改的 5 文件
- ✅ Mavis (修 1 compile error) — 0 触碰

---

**R121r-5 完. 任务 4 ✅ PASS (jitter 6 test + eviction 6 test). R121r-6 立即开干 (任务 5: dependabot auto-merge yml).**
