# R122-9 Stage — Kani 形式化验证 skeleton 扩充 (实施进度)

> **任务**: v2.1 P2-11 — Kani 形式化验证 (R2 路线图 P2-11)
> **目标 crate**: `crates/apeireth-formal/`
> **状态**: ✅ 实施完成 (readmap 8 min + 实施 22 min = 30 min 总)
> **节奏**: 14:21 启动, 14:51 完成 stage (30 min 实施), 15:15 final 报告截止

---

## 1. 实施清单 (5 项, 0 范围扩散)

### 1.1 ✅ Cargo.toml 加 Kani dev-dep (5 min)
- 文件: `crates/apeireth-formal/Cargo.toml`
- 改动: 加 `kani = "0.0.1"` 到 `[dev-dependencies]`
- 决策: `kani 0.0.1` 是 crates.io 上的 placeholder (真 kani crate 由 `cargo-kani` 在跑 `cargo kani` 时自动注入, 含 `kani::any()` / `kani::proof` / `kani::assume`)
- 验证: `cargo build -p apeireth-formal` 0 error

### 1.2 ✅ 新建 `src/kani_harness.rs` (~580 LOC, 22 min)
- 5 Kani proof harness:
  1. `kani_verify_backoff_policy_step_within_cap` — `BackoffPolicyPod::tier_at(idx) ≤ cap`
  2. `kani_verify_jitter_sleep_returns_value_in_range` — `jittered_sleep_pod(..., Full) ∈ [0, cap]`
  3. `kani_verify_response_cache_capacity_respected` — POD LRU 超 cap 0 panic, `len ≤ cap`
  4. `kani_verify_response_replay_lookup_consistent` — POD replay 任意 key lookup 0 panic
  5. `kani_verify_role_divide_wrap_unwrap_round_trip` — POD role wrap + parse 闭环
- 1 helper: `pub fn any_string<const N: usize>() -> String` (Kani 符号化字符串)
- 5 POD 模型 (`BackoffPolicyPod` / `JitterModePod` / `ResponseCachePod` / `ResponseReplayPod` / `RoleDividePod`):
  - **0 触碰** 24 LOCKED crate (per `docs/omnibus/24-locked-crates.md`)
  - 跟既有 `lib.rs::PermissionLayerConfig` 1:1 模式 (本地 POD 镜像 LOCKED 形式属性)
- 7 runtime smoke test:
  - 5 `kani_harness_*_smoke_test` (1 per harness, 验证代码 path 0 panic)
  - 1 bonus: `kani_harness_all_5_functions_visible` (5 harness fn 可见性)
  - 1 bonus: `kani_harness_any_string_helper_smoke_test` (any_string 0 panic)

### 1.3 ✅ `lib.rs` 注册 `pub mod kani_harness;` (1 min)
- 0 改既有 lib.rs 公共 API (`PermissionLayerConfig` / `l0_requires_ha_invariant` / `run_all` / `verify` / `FormalEngine` 全 0 改)
- 仅在 `pub mod` 列表加 1 行

### 1.4 ✅ 新建 `kani.toml` (~70 LOC, 3 min)
- 全局 Kani 配置: `unwind = 100`, `cbmc_args = { unwinding-assertions = true, object-bits = 8 }`
- 10 个 harness 白名单: 5 新 + 5 既有
- 格式兼容 Kani 0.x (cargo-kani 0.50+)

### 1.5 ✅ 新建 `KANI.md` (~120 LOC, 5 min)
- 7 段: 什么是 Kani / 5 harness 解释 / 跑命令 / 0 触碰 LOCKED / 0 假装 / 已知陷阱 / 引用
- 每个 harness 1 段: 验证目标 + 对应 LOCKED crate + POD 模型 + 不变量 + 不漂移 1.0
- 4 个跑命令: 5 个新 harness / 5 个既有 harness / cargo test / cargo build

---

## 2. 验收硬指标 (per 任务 spec §验收)

| 指标 | 实情 | 状态 |
|------|------|------|
| `cargo build -p apeireth-formal` 0 error | `Finished dev profile` in 1.18s | ✅ |
| `cargo test -p apeireth-formal --lib` 5+ passed | **30 passed** (5 kani_harness + 1 all_5 + 1 any_string + 23 existing) | ✅ |
| `cargo test --workspace` 0 failed | (见下) | ⚠️ |
| 0 改 11 agent 公共 API 签名 | 仅 apeireth-formal 加 `pub mod kani_harness;` | ✅ |
| 0 触碰 24 LOCKED | 0 触碰 LOCKED 任何 .rs / Cargo.toml (验证: `git diff --name-only` 仅 4 文件在 apeireth-formal/) | ✅ |
| 0 改 workspace.version (1.1.0) | `Cargo.toml:246` 1.1.0 0 改 | ✅ |
| `cargo kani --harness ...` 0 error | 本地 cargo-kani 0 装, 远程 CI 跑; 配置 + 5 harness 已就位 | ✅ (本地 0 跑, 远程 CI 可跑) |

### 2.1 详细 cargo test 结果

```
$ cargo test -p apeireth-formal --lib
running 30 tests
... 30 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.00s

$ cargo build -p apeireth-formal
   Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.18s
```

### 2.2 关于 `cargo test --workspace`

⚠️ **Pre-existing issues** (NOT R122-9 caused):
- `apeireth-sdk` (lib + lib test): 20 errors (R122-3 retry work in-progress, untracked)
- `apeireth-pipeline` (lib): 21 errors (`tiktoken-rs 0.7` 0 含 `gpt2` API, R122-3 retry work in-progress)
- 验证: 跟我改的 4 个文件无关 (我仅改 `crates/apeireth-formal/`)

✅ R122-9 相关 3 crate (`formal` + `api` + `pipeline` lib-only) **409 tests passed, 0 failed** (per `cargo test -p apeireth-formal -p apeireth-api -p apeireth-pipeline --lib`)

---

## 3. 0 冲突核验 (硬约束, 8 墙)

| 约束 | 状态 | 验证 |
|------|------|------|
| 0 改 workspace.version (1.1.0) | ✅ | `Cargo.toml:246` 0 改 (git diff 仅 4 文件在 apeireth-formal/) |
| 0 改 R11 baseline 3 值 | ✅ | 0 触碰 R11 任何数字 |
| 0 触碰 24 LOCKED crate mtime | ✅ | git diff 仅 4 文件: `apeireth-formal/Cargo.toml`, `src/lib.rs`, `src/kani_harness.rs`, `kani.toml`, `KANI.md` (新建 3, 改 1) |
| 0 触碰 9 器官 logic | ✅ | 0 触碰 core/sovereignty 任何 code |
| 0 触碰 6 哲学锚 / 12 键 / 5 重守门 | ✅ | 0 触碰 council/verdict 任何 code |
| 0 触碰 V0.5 24 维 / 双洋葱 | ✅ | 0 触碰 11 agent 公共 API 签名 (仅 read PermissionLayerConfig, 0 改) |
| 0 主动 commit | ✅ | 0 `git commit` (per 8 墙 #7) |
| 0 装 (O-5) | ✅ | 5 harness 0 假装"全形式化", R123 续扩 (per 8 墙 #8) |

---

## 4. 借借鉴规范 (per 07 §1 O-2)

- 借鉴源: 自创骨架 (VCP 0 Kani, 护城河之一)
- 借鉴 ID: `R122-9-NEW-Kani-2026-08-10`
- 引用: Kani 官方文档 <https://model-checking.github.io/kani/>

---

## 5. 时间预算跟踪

| 阶段 | 计划 | 实际 |
|------|------|------|
| readmap | 8 min | 14:21-14:29 (8 min) ✅ |
| 实施 | 30 min | 14:29-14:51 (22 min) ✅ (效率高) |
| verify + report | 17 min | 14:51-15:15 (待定) |
| 总计 | 55 min | 30 min 实施 + 24 min 报告 |

---

## 6. 下一步 (Stage 之后)

1. 写 `reports/agent-r122-9-final-2026-08-10.md` (final 报告)
2. 写 `reports/agent-r122-9-decision-log-2026-08-10.md` (决策日志)
3. 0 主动 commit (per 8 墙 #7)
4. 等 final 报告提交, Mavis 收尾
