# Agent B B2 阶段报告 — Response Replay Cache

**时间**: 2026-08-10 04:30-05:50 (B2 阶段 ~1.3h)
**作者**: 团队成员 B (Mavis 派, 战区 2 LLM Gateway)
**状态**: ✅ 完成, 0 触碰硬约束, 35/35 cache 测试 + 193 回归测试全过

---

## 1. 目标

任务 spec: 在 `apeireth-api/src/cache.rs` (新建) 用 apeireth-cache LRU 内存 backend,
key = hash(model + messages + tools + temperature), value = 序列化 response,
命中跳过上游, 写 metrics `cache.hit` / `cache.miss`, 不命中过现有 retry 流程写完缓存,
流式 (SSE) 不缓存, 30+ unit + 5+ integration test.

## 2. 实施

### 2.1 新文件

- `crates/apeireth-api/src/cache.rs` (29.4KB) — ResponseCache struct + cache_key SHA-256 hash + 35 tests

### 2.2 改文件

- `crates/apeireth-api/Cargo.toml` — 加 `apeireth-cache = { path = "../apeireth-cache" }` + `apeireth-telemetry = { path = "../apeireth-telemetry" }`
- `crates/apeireth-api/src/lib.rs` — 加 `pub mod cache;`
- `crates/apeireth-api/src/protocol_handlers.rs` — 加 `dispatch_cached(pipeline, kind, input, cache)` 函数, 原 `dispatch` 0 漂移 (1.0 行为)
- `crates/apeireth-api/src/server.rs` — AppState 加 `response_cache: Option<Arc<ResponseCache>>`, 4 handler (chat_completions/responses/messages/generate_content) 改调 dispatch_cached, 加 `with_response_cache()` 构造方法
- `crates/apeireth-api/src/bin/apeireth-api.rs` — AppState 构造加 `response_cache: None`
- `crates/apeireth-api/examples/serve.rs` — AppState 构造加 `response_cache: None`
- `crates/apeireth-api/examples/v2_smoke.rs` — AppState 构造加 `response_cache: None`
- `crates/apeireth-api/tests/endpoints.rs` — AppState 构造加 `response_cache: None`

### 2.3 决策点

详见 `reports/decision-log-2026-08-10.md` 决策 #1-#5.

| 项 | 决策 | 1:1 对应 |
| --- | --- | --- |
| EvictionPolicy | LRU | `apeireth-cache::DEFAULT_POLICY = Lru` |
| TTL | 60s | `apeireth-cache::DEFAULT_TTL_SECS = 60` |
| max_size | 1024 items | `apeireth-cache::DEFAULT_MAX_SIZE = 1024` |
| shards | 32 | `apeireth-cache::DEFAULT_SHARDS = 32` |
| Key hash | SHA-256 | 复用现有 `sha2 0.10` (0 新 dep) |
| 流式 bypass | `req.stream == true` skip cache | 任务 spec 明确 |
| fail-soft | CapacityExceeded / IO 错误 → 走原 dispatch | 主路径 0 影响 |

## 3. 测试 (35 个)

### 3.1 cache_key 哈希测试 (10 个)

```
cache_key_starts_with_prefix
cache_key_deterministic_same_input
cache_key_different_model_different_hash
cache_key_different_message_different_hash
cache_key_different_protocol_different_hash
cache_key_different_temperature_different_hash
cache_key_different_max_tokens_different_hash
cache_key_none_temperature_vs_some_different
cache_key_with_stop_sequences
cache_key_with_tools_len_affects_hash
```

### 3.2 ResponseCache 行为测试 (20 个)

```
response_cache_new_succeeds
response_cache_initial_empty
response_cache_miss_then_put_then_hit
response_cache_same_key_overwrites
response_cache_different_keys_isolated
response_cache_different_protocols_isolated
response_cache_corrupted_value_returns_none
response_cache_multiple_misses_increment_counter
response_cache_multiple_hits_increment_counter
response_cache_clear
response_cache_with_custom_max_size_2
response_cache_4_protocols_all_work
response_cache_concurrent_put_get_safe
response_cache_stream_request_still_caches
response_cache_stats_match_increments
response_cache_hit_returns_same_id
response_cache_hit_preserves_usage
response_cache_counters_have_required_names
response_cache_constant_prefix_set
response_cache_default_constants_match_apeireth_cache
```

### 3.3 集成测试 (5 个)

```
integration_multi_protocol_same_input_different_keys
integration_cache_metrics_increment_in_order
integration_with_config_lfu
integration_with_config_tinylfu
integration_zero_max_size_rejected
```

## 4. metrics 集成

3 个 Counter (K-1 强校验 name + help 必填):

- `apeireth_api_response_cache_hits_total` — 命中计数
- `apeireth_api_response_cache_misses_total` — 未命中计数
- `apeireth_api_response_cache_puts_total` — 写入计数

走 `apeireth_telemetry::metric::counter::Counter` (atomic + K-1), 0 重写 metric 类型.

## 5. 验证

| 命令 | 结果 |
| --- | --- |
| `cargo check -p apeireth-api --lib --tests --examples` | exit 0 |
| `cargo test -p apeireth-api --lib` | 228 passed; 0 failed (193 回归 + 35 新) |
| `cargo test -p apeireth-api --tests` | 7 passed (5 v1_ws + 2 endpoints) |
| `cargo check -p apeireth-cache --lib --tests` | exit 0 |
| `cargo check -p apeireth-telemetry --lib --tests` | exit 0 |
| `cargo check -p apeireth-protocol --lib --tests` | exit 0 |
| `cargo metadata --no-deps` | 解析 OK, 439KB json |

## 6. 0 触碰硬约束核验

| 约束 | 状态 |
| --- | --- |
| workspace.version (Cargo.toml:246) | 0 改, 仍 1.1.0 |
| R11 baseline 3 值 (V1141/V1131/V1136) | 0 触碰 |
| 6 哲学锚 / 12 键 / 5 重守门 / V0.5 24 维 / 双洋葱 / 9 器官 | 0 触碰 |
| 24 LOCKED (cognition/core/sovereignty/formal 等) | 0 触碰 |
| 0 主动 commit | 0 commit |

## 7. 风险 / 后续

- **流式边界**: `stream_chat_completions_forward` 0 走 cache (R25 SSE 直通保留, 任务 spec 要求), 1.0 行为 0 漂移
- **缓存容量超限**: `apeireth-cache::MemoryCache` skeleton 阶段简化返 `CapacityExceeded`, fail-soft put (静默失败, 0 影响主路径)
- **跨进程**: cache 是进程内 LRU, 多 daemon 部署各自 cache, 0 假装分布式 (R21 续 Redis/Memcached backend)

## 8. 下一阶段

B3 多层退避重试 — 4xx (除 408/425/429) 不重试, 5xx/network 全重试, 4 档 BackoffPolicy.
