# R123-2 Final — 4 协议 handler 抽 trait 抽象

**Date**: 2026-08-10
**Coder**: R123-2 (R122-11 重派, 任务工具 14:42 Connection error 挂, Mavis 重派)
**Slot**: v2.1 抽 trait 抽象, R122-10 5 重复模式 #1
**借鉴 ID**: `R123-2-NEW-ProtocolHandlerTrait-2026-08-10` (0 VCP 借鉴, 自创骨架)
**Readmap**: `reports/agent-r123-2-readmap-2026-08-10.md`
**Decision Log**: `reports/agent-r123-2-decision-log-2026-08-10.md`

---

## §0. TL;DR

完成 v2.1 任务 #1: 4 协议 handler 抽 `ProtocolHandler` trait 抽象, 1 个 `HandlerRegistry` 注册表 +
1 个 `route_dispatch` 通用模板 + 8 unit test, 加 1 example 演示 4 协议注册 + dispatch.

- **0 范围扩散**: 仅在 `apeireth-api` 1 个 crate 加 1 新 mod + 1 lib.rs 行 + 1 example + 1 Cargo.toml 块
- **0 装"已替换"**: 0 接入真 4 协议 handler impl, 0 触碰现有 `protocol_handlers.rs` / `v2_endpoints.rs` / `server.rs`
- **0 触碰 LOCKED**: workspace.version 仍是 1.1.0, 0 改 R11 baseline, 0 触碰 24 LOCKED crate
- **0 主动 commit**: 改文件, 0 git add / 0 commit (等主人 Mavis 统一收尾)

---

## §1. 交付清单 (4 文件)

| # | 路径 | 状态 | 行数 | 内容 |
|---|------|------|------|------|
| 1 | `crates/apeireth-api/src/protocol_handler_trait.rs` | NEW | 470 | trait + registry + route_dispatch + 8 test |
| 2 | `crates/apeireth-api/src/lib.rs` | +2 行 | — | `pub mod protocol_handler_trait;` 声明 |
| 3 | `crates/apeireth-api/Cargo.toml` | +5 行 | — | 1 个 `[[example]]` 块 |
| 4 | `crates/apeireth-api/examples/protocol_handler_demo.rs` | NEW | 140 | 4 stub handler 注册 + dispatch demo |

**总 diff**:
- 2 existing files: +7 行 (Cargo.toml 5 + lib.rs 2)
- 2 new files: 610 行 (1 trait mod + 1 example)
- 0 deleted files

---

## §2. 验收硬指标 (全过)

| # | 指标 | 命令 | 结果 |
|---|------|------|------|
| 1 | `cargo build -p apeireth-api` 0 error | `cargo build -p apeireth-api` | ✅ `Finished dev profile in 1m 10s` (359 pre-existing warnings, 0 error) |
| 2 | `cargo test -p apeireth-api --lib protocol_handler_trait_tests` 8+ passed | `cargo test -p apeireth-api --lib protocol_handler_trait_tests` | ✅ **8 passed; 0 failed** |
| 3 | `cargo test -p apeireth-api --lib` 全 0 failed (319 + 8 = 327+) | `cargo test -p apeireth-api --lib` | ✅ **327 passed; 0 failed; 0 ignored** (319 旧 + 8 新 = 327) |
| 4 | `cargo check --workspace` 0 failed | `cargo check --workspace` | ✅ `Finished dev profile in 4.74s` (0 error) |
| 5 | 0 改 11 agent 公共 API 签名 | `git diff` | ✅ 仅 lib.rs +2 行 mod 声明, 0 改任何 fn 签名 |
| 6 | 0 触碰 24 LOCKED crate | `git status` | ✅ 0 触碰 (我仅 apeireth-api, 其他 modified files 是 R123-1 平行工作) |
| 7 | 0 改 workspace.version (1.1.0) | `git diff Cargo.toml` | ✅ 1.1.0 不变 (我 +5 行仅 example 块) |
| 8 | 0 装 (O-5) | `git diff protocol_handlers.rs v2_endpoints.rs server.rs` | ✅ 0 触碰 3 现有文件, 0 接入真 handler, R123+ 续 |

**Bonus 验证**:
- ✅ `cargo build -p apeireth-api --example protocol_handler_demo` 0 error
- ✅ `cargo run -p apeireth-api --example protocol_handler_demo` 跑成功, 输出 4 协议 register + 3 gateway Err 0 漂移

---

## §3. 8 Unit Test 清单 (per spec §2)

| # | test 名 | 验证内容 |
|---|---------|----------|
| 1 | `protocol_handler_trait_endpoint_url_returns_static_str` | impl stub → `endpoint_url()` 返 4 协议各自 path, 跟 `protocol_handlers` const 对齐 |
| 2 | `protocol_handler_trait_cache_key_stable_for_same_input` | 同一 `NormalizedRequest` → 同 `cache_key`; 不同 model / msg_count → 不同 key |
| 3 | `protocol_handler_trait_supports_stream_default_true` | 不重写 → 默 true; 重写 false → 返 false |
| 4 | `protocol_handler_trait_dispatch_routes_to_handler` | `route_dispatch(&h, req)` → 调 h.dispatch, 返正确 content + id |
| 5 | `handler_registry_register_4_protocols_4_handlers` | register 4 协议 stub → `len() == 4`, `is_empty() == false` |
| 6 | `handler_registry_dispatch_4_protocols_returns_correct` | 4 协议 dispatch → 各自 content 互不混 (4 unique HashSet) |
| 7 | `handler_registry_dispatch_unknown_kind_returns_error` | Acp/Mcp/OpenClawGateway 0 注册 → Err 含 "no handler registered" + "kind" |
| 8 | `protocol_handler_trait_send_sync_compiles` | 编译期: `Box<dyn ProtocolHandler + Send + Sync>` 是 Send + Sync |

**0 依赖任何具体 protocol impl** (per spec §2 要求):
- ✅ 用 `StubHandler { endpoint, prefix, supports_stream_flag }` 1 个本地 stub, 4 协议统一走
- ✅ 0 import `protocol_handlers::*` 任何 fn
- ✅ 0 import `v2_endpoints::*` 任何 fn
- ✅ 0 调 `dispatch_cached_with_status` / `dispatch_with_retry` / `stream_forward` / `parse_protocol_kind` 等 11 公共 API

---

## §4. 架构位置 (R123+ 续真接后)

```
客户端 4 协议请求
  ↓
server.rs 4 endpoint                              (R123+ 续替换点)
  ↓ registry.dispatch(kind, req)                   ← 本 trait 注册表入口
HandlerRegistry { HashMap<ProtocolKind, Box<dyn ProtocolHandler + Send + Sync>> }
  ↓ route_dispatch(handler, req)                   ← 本 trait 通用模板
ProtocolHandler (4 impl: OpenAiChat / OpenAiResponses / AnthropicMessages / Gemini)  ← R123+ 续真接
  ↓ handler.dispatch(req)                          ← 派发归一化请求
NormalizedResponse                                ← 跟 1.0 NormalizedResponse 同型, 0 漂移
```

**当前 1.0 (R17 战役 1-4 至今)**: server.rs 4 endpoint → `protocol_handlers::dispatch_cached_with_status` → 5 步管线
**R123+ 续**: server.rs 4 endpoint → `HandlerRegistry::dispatch` → `route_dispatch` → `ProtocolHandler::dispatch` (impl 由 R123+ 续接 `dispatch_cached_with_status`)

**0 漂移 1.0 行为**: 本 trait `dispatch` 返 `Result<NormalizedResponse, String>`, 跟现有
`protocol_handlers::dispatch` 1.0 行为 0 漂移 (避免 0 假装引 `ProtocolError`)

---

## §5. 0 触碰核验 (per spec §硬约束)

| # | 硬约束 | 状态 | 证据 |
|---|--------|------|------|
| 1 | 0 改 workspace.version | ✅ | `Cargo.toml:246 version = "1.1.0"` 不变 |
| 2 | 0 改 R11 baseline 3 值 | ✅ | 0 触碰 `apeireth-asi/tests/integration_r_measure.rs` |
| 3 | 0 触碰 24 LOCKED crate mtime | ✅ | 0 触碰 (我仅 `apeireth-api` 内 4 文件) |
| 4 | 0 触碰 9 器官 logic | ✅ | 0 触碰 `apeireth-core/src/{9 organ mods}` |
| 5 | 0 触碰 6 哲学锚 / 12 键 / 5 重守门 / V0.5 24 维 / 双洋葱 | ✅ | 0 触碰任何 philosophical anchor crate |
| 6 | 0 改 11 agent 公共 API 签名 | ✅ | 0 触碰 `protocol_handlers.rs` / `v2_endpoints.rs` / `server.rs` / `replay_cache.rs` / `cache.rs` / `retry.rs` / `routing.rs` / `cache.rs` / `evictor.rs` / `BackoffPolicy` / `JitterMode` / `KeyPathSpan` |
| 7 | 0 主动 commit | ✅ | `git status` 显示 4 改动, 但 0 `git add` / 0 `git commit` |
| 8 | 0 装 (O-5) | ✅ | 0 接入真 4 协议 handler impl, 0 假装"已替换" |

**`git status` 我的 4 件改动**:
```
M  crates/apeireth-api/Cargo.toml                    (+5 行, 1 example 块)
M  crates/apeireth-api/src/lib.rs                    (+2 行, 1 mod 声明)
?? crates/apeireth-api/examples/protocol_handler_demo.rs  (NEW)
?? crates/apeireth-api/src/protocol_handler_trait.rs       (NEW)
```

**其他 modified files** (R123-1 / 其他 agent 平行工作, 0 归 R123-2):
```
M  crates/apeireth-asi/src/measurement.rs       (R123-1)
M  crates/apeireth-mcp/Cargo.toml                (R123-1)
M  crates/apeireth-mcp/src/lib.rs                (R123-1)
D  crates/apeireth-mcp/src/tools.rs              (R123-1)
?? crates/apeireth-mcp/examples/...              (R123-1)
M  crates/apeireth-tui/src/theme.rs              (其他)
```

---

## §6. 时间 (15:50 启动 → 16:?? 完成)

| 段 | 时长 | 内容 |
|----|------|------|
| Readmap | 5 min | 读 lib.rs / Cargo.toml / R122-10 refactor / protocol_handlers.rs / apeireth-protocol 类型 |
| 实施 | 50 min | 写 4 文件 (1 trait mod 470 行 + 1 example 140 行 + 2 处增改 +7 行) + 2 轮编译错误修复 (ContentPart::text → text_only, content: Vec<...>) |
| Verify | 8 min | build + test + check + example run + git diff 0 触碰核验 |
| Report | 7 min | readmap + final + decision log (本文件) |

**总: ~70 min**, 在 1h10m 预算内.

---

## §7. 借鉴规范核验 (per 07 §1 O-2)

- ✅ 借鉴 ID 显式声明: `R123-2-NEW-ProtocolHandlerTrait-2026-08-10`
- ✅ 0 VCP 借鉴 (VCP 无 4 协议 handler trait 抽象, VCP 用 4 个独立 module)
- ✅ 自创骨架, 0 抄 VCP 真代码
- ✅ 0 装"已实现" (主哲学锚 #1)

---

## §8. R123+ 续任务 (明确下游, 0 派活)

R123-2 仅"骨架 ready, 0 接入", 后续 2 阶段 R123+ agent 续干:

**R123+ 阶段 1**: 接真 4 协议 handler impl
- `impl ProtocolHandler for OpenAiChat { fn dispatch = |req| protocol_handlers::dispatch_cached_with_status(pipeline, OpenAiChat, req, Some(cache)).await }`
- 同样 OpenAiResponses / AnthropicMessages / Gemini
- 0 改 `protocol_handlers::dispatch_cached_with_status` 任何 fn 签名

**R123+ 阶段 2**: server.rs 4 endpoint 切换
- `app.post("/v1/chat/completions", |req| registry.dispatch(OpenAiChat, req))`
- 0 改 4 协议 URL path / 0 改 4 协议 req/resp JSON shape (1.0 行为 0 漂移)

**R123+ 阶段 3**: route_dispatch 接入 cache
- 加 `let key = handler.cache_key(&req); if let Some(cached) = cache.get(&key) { return Ok(cached); }`
- 0 改 `ResponseCache` / `ReplayCache` 任何 fn 签名

---

## §9. 不假装 (主哲学锚 #1)

- ✅ 1 trait + 1 registry + 1 route_dispatch 模板 ready (4 文件 +7 行增量)
- ✅ 8 test 真跑, 0 装 "test written"
- ✅ HandlerRegistry 真用 `HashMap<ProtocolKind, ...>`, 0 装 "AnyMap"
- ✅ route_dispatch 当前 1:1 调 `handler.dispatch`, 0 装 "已接 cache lookup"
- ✅ example 真跑 (`cargo run` 跑成功, 4 协议 + 3 gateway Err 0 漂移)
- ✅ 4 协议接入是 R123+ 续, 本任务仅"骨架 ready", 0 假装"已替换"

---

**R123-2 完成 (Mavis 自干, 0 范围扩散, 0 装, 0 越界 commit, 等主人收尾).**
