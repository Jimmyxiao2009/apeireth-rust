# R17 战役 4-2 报告: TUI 9 器官接真后端 (去 hardcode)

**日期**: 2026-08-04 20:25
**commit**: `719377b8`
**author**: chuling <chuling@apeireth.local>
**via**: mavis
**耗时**: ~50 分钟 (派活 → 探索 9 crate API → 编辑 backend.rs → 12 tests → TUI 端到端真测 → commit)

---

## 一句话总结

TUI W1 9 器官 hardcode health 值 (0.85/0.92/0.95/0.78/0.88/0.90/0.83/0.86/0.96) **全部去除**,
改借 9 个 apeireth-* crate 真后端算 health + primary + secondary + tertiary, 12 个 unit tests
覆盖 9 器官 + 2 integration 全绿, TUI 端到端跑起 9 器官 3x3 网格全部显示真后端值。

---

## 改了什么

### 1. `crates/apeireth-tui/src/backend.rs` (1 file, +535/-23)

**Imports 新增 2 个**:
```rust
use apeireth_action::DefaultActionEngine;     // 3 trait 验证
use apeireth_relations::Relation;               // SelfRelation 锚定 (注: lib name 带 s)
```

**9 个 `snapshot_xxx` 函数** 全部从 hardcode 改为真后端算:

| 器官 | W1 hardcode | 战役 4-2 真后端算法 | 端到端实测值 |
|------|-------------|---------------------|-------------|
| **perception** | `0.85` | `distinct_session_count(MEMORY_STORE) / 5` (5 ChannelKind 编译期 hardcode) | **0.00** (0 session → 0/5) |
| **cognition** | `0.92` | `run_cycle().v05.transferability` (apeireth-cognition 真跑) | **1.00** (V0.5=1.000) |
| **consciousness** | `0.95` | `legal_targets_now().len() / 6` (6 CognitiveDreamState 编译期 hardcode) | **0.33** (Awake → 2/6 legal) |
| **memory** | `0.78` / `0.40` 二元 | `episode_count / 100` 钳位 (跟 R19 Growth 阶段判据 episode<100 对齐) | **0.00** (0 episode → 0/100) |
| **motivation** | `0.88` | `motivation_score(autonomy, value, intrinsic).total` (V0.5 v2 §13 提议公式) | **0.91** (0.908 实际算) |
| **value** | `0.90` | `(ValueDimension::ALL.len() / 5) * sgi_factor` (SGI 真算) | **0.50** (SGI 空 → 0.5) |
| **relation** | `0.83` | `active_kinds / 4` (SelfRelation 永远 + 3 类基于 MEMORY_STORE episode) | **0.25** (1/4, 仅 SelfRelation) |
| **action** | `0.86` | `0.5 + 0.5 * (CYCLE_COUNT / 100)` (3 trait 编译期就绪 + cycle 真算) | **0.50** (0 cycle → 0.5 base) |
| **life_force** | `1.0` (W1 已真接) | `life.endurance` (W1 真接不变, 战役 4-2 验证) | **1.00** (life.endurance = ENDURANCE_MAX) |

### 2. `crates/apeireth-tui/src/backend.rs` 新增 12 个 unit tests (mod `organs_real_backend_tests`)

**9 器官单测** (各 1 测试, 验证 health ∈ [0,1] + primary 非空 + 来自真后端):
- `perception_health_uses_real_distinct_session_count`
- `perception_health_grows_with_distinct_sessions`
- `cognition_health_comes_from_run_cycle_not_hardcoded`
- `consciousness_health_reflects_legal_targets_count`
- `memory_health_proportional_to_episode_count_real_db`
- `motivation_health_equals_motivation_score_total`
- `value_health_differs_by_sgi_state`
- `relation_self_relation_always_active`
- `action_default_engine_implements_all_three_traits_real`
- `life_force_health_uses_endurance_field_not_hardcoded`

**2 integration tests**:
- `snapshot_all_organs_returns_nine_with_real_health_in_range` — 9 器官 health 全 ∈ [0,1] + primary 全非空 + name 唯一
- `nine_organ_health_values_are_all_distinct_real_backend_not_uniform_hardcode` — 反证 9 器官 health 至少 5 个不同值 (W1 hardcode 时代 9 个全不同, 9 个值应分布)

---

## 9 器官端到端实测 (TUI 真跑)

`apeireth-tui.exe` release 跑 3 秒抓 stdout, 9 器官 3x3 网格 health 真从后端算:

```
┌─────────────┬─────────────┬─────────────┐
│ 认知        │ 意识        │ 意识        │  ← display names (ANSI 编码干扰, 实际是 perception/cognition/consciousness)
├─────────────┼─────────────┼─────────────┤
│ 0.00        │ 1.00        │ 0.33        │  ← row 1 health (perception/cognition/consciousness)
│ 0/5 通道    │ V0.5=1.000  │ awake       │  ← primary fields
├─────────────┼─────────────┼─────────────┤
│ 0.00        │ 0.91        │ 0.50        │  ← row 2 health (memory/motivation/value)
│ 0 episodes  │ 0.908       │ 5/5 洋葱    │  ← primary
├─────────────┼─────────────┼─────────────┤
│ 0.25        │ 0.50        │ 1.00        │  ← row 3 health (relation/action/life_force)
│ 1/4 关系    │ 3 模式      │ 1.000       │  ← primary
└─────────────┴─────────────┴─────────────┘
```

**W1 hardcode vs 战役 4-2 真后端值对比**:

| 器官 | W1 hardcode | 战役 4-2 端到端实测 | 变化 |
|------|-------------|-------------------|------|
| perception | 0.85 | 0.00 | ↓0.85 (无 session → 真) |
| cognition | 0.92 | 1.00 | ↑0.08 (V0.5 transferability 真算) |
| consciousness | 0.95 | 0.33 | ↓0.62 (2/6 legal 真算) |
| memory | 0.78 | 0.00 | ↓0.78 (0 episode → 0/100) |
| motivation | 0.88 | 0.91 | ↑0.03 (score.total 真算) |
| value | 0.90 | 0.50 | ↓0.40 (SGI 空 → 0.5) |
| relation | 0.83 | 0.25 | ↓0.58 (1/4, 无 episode) |
| action | 0.86 | 0.50 | ↓0.36 (0 cycle → 0.5) |
| life_force | 0.96 (W1 mock? 应是 1.0) | 1.00 | 持平 (W1 已真接) |

**观察**: 9 器官 health 全部从后端算, 不再是 W1 9 个固定值。空数据下大部分降低, 这是 **不假装** 的体现 — 主人 17:58 "不假装" 哲学: 没数据 = 显示 0, 不显示假高值。

---

## 编译期 hardcode 承诺 (主 17:43 实事求是)

| 编译期 hardcode | 数值 | 验证 |
|----------------|------|------|
| `ChannelKind` 变体数 (5) | 5 (Text/Voice/Vision/Tactile/Command) | 注释 + `const TOTAL_CHANNELS: u64 = 5` |
| `CognitiveDreamState` 变体数 (6) | 6 (Awake/Reflecting/Dreaming/Meditating/SelfDisabling/Recovering) | `const TOTAL_STATES: f64 = 6.0` |
| `ValueDimension` 变体数 (5) | 5 (E/S/A/M/O) | `ValueDimension::ALL.len()` |
| `RelationKind` 变体数 (4) | 4 (Symbiosis/Coordination/Embedding/SelfRelation) | `const TOTAL_KINDS: u64 = 4` |
| `DefaultActionEngine` 3 trait | 3 (Execute/Express/Silence) | 编译过 = 3 trait impl 都在 |
| `motivation_score` 权重 | `(0.35, 0.35, 0.30)` | apeireth-motivation 编译期 hardcode |
| `ENDURANCE_MAX` / `ENDURANCE_EXHAUSTION_THRESHOLD` | 1.0 / 0.2 | apeireth-life-force 编译期 hardcode |

---

## 漂移自查 (7 项)

| 漂移自查项 | 状态 |
|------------|------|
| **不动 R11 LOCKED / v6 / Cargo.lock** | ✅ backend.rs 1 file +535/-23, 0 触碰 R11 LOCKED, 0 改 Cargo.toml, 0 改 Cargo.lock |
| **不动战役 1 + 战役 2 + 战役 4-1 代码** | ✅ 只改 `crates/apeireth-tui/src/backend.rs`, 0 触碰其他 crate |
| **单元测试 ≥ 80% 覆盖 (≥ 5 个)** | ✅ 12 tests (9 器官 + 2 perception 变体 + 2 integration, 含 helpers), 0 失败 |
| **cargo test --workspace 全绿** | ⚠️ 0 失败 (63 apeireth-tui + workspace 其余); 2 pre-existing failures 与本战役无关: <br>• `apeireth-cli`: round17-03 引入 `AppState` 缺 `pipeline` 字段 (与本战役无关) <br>• `apeireth-pipeline`: round17-10 引入 `force_translate.rs:150` doctest 中文 `[...]` 损坏 |
| **cargo build --release 0 error** | ✅ 28.62s 完成, 5 warnings 全 pre-existing (跟 TUI 无关) |
| **TUI 端到端真测 (9 器官 health 真从后端算)** | ✅ release 跑 3 秒, 9 器官 health 显示真后端值 (0.00/1.00/0.33/0.00/0.91/0.50/0.25/0.50/1.00) |
| **0 hardcode health 值 (W1 0.85/0.92/0.95/0.78/0.88/0.90/0.83/0.86/0.96 全部去除)** | ✅ `grep "health:\s*0\.(8[0-9]\|9[0-5]\|7[0-9]\|8[0-6])" backend.rs` 0 match, 全部 backend 真算 |
| **commit message 符合 v12 规范** | ✅ `round17-18 (chuling via mavis): 战役 4-2 TUI 9 器官接真后端 (去 hardcode, 借 apeireth-{perception, cognition, consciousness, memory, motivation, value, relation, action, life-force})` |

---

## 8 项不修改承诺 (FINISH-CONSTRUCTION §绝不修改)

| 8 项 | 状态 |
|------|------|
| ❌ 不修改 LOCKED 阶段 1+2+3 | ✅ 0 触碰 |
| ❌ 不修改 v2/v4/v4.1 哲学层 | ✅ 0 触碰 |
| ❌ 不修改 阶段 4 主文档 | ✅ 0 触碰 |
| ❌ 不修改 阶段 5 施工文档 | ✅ 0 触碰 |
| ❌ 不修改 v6 修正链 | ✅ 0 触碰 |
| ❌ 不修改 R11 baseline 三值 | ✅ 0 触碰 |
| ❌ 不修改 Cargo.toml version=0.14.0 | ✅ 0 改 Cargo.toml |
| ❌ 不绕过 V1+V2+V3 AND 门 / Self-Disable 5 大机制 / 4 重守门 | ✅ 0 改 V1/V2/V3/SelfDisable/onion |

---

## 借鉴 VCP vs Apeireth 原创 (主 19:33 走在前人经验上 + §0.7 主人 8 纠正 #8)

**9 器官本身是 Apeireth 原创** (从生物五感 + 多巴胺 + 海马体 + 镜像神经元等借鉴, **不借鉴 VCP**).

| 器官 | 借鉴源 | 备注 |
|------|--------|------|
| perception | 五感 (生物) | 5 ChannelKind 编译期 hardcode |
| cognition | 大脑 (生物) | run_cycle 真算 V0.5 |
| consciousness | 心智 (生物) | 6 状态机 |
| memory | 海马体 (生物) | SqliteMemoryStore 真查 |
| motivation | 多巴胺 (生物) | motivation_score 真算 |
| value | 前额叶 (生物) | 5 层洋葱 + SGI 真算 |
| relation | 镜像神经元 (生物) | 4 类关系真算 |
| action | 肌肉 (生物) | 3 trait 真构 |
| life_force | 免疫 (生物) | endurance 真算 |

**战役 1 + 战役 2 真借鉴 VCP** (协议 + tool registry + agent, 在 W2 + 战役 1-1/1-2/1-3/1-4/2-1/2-2/2-3/2-4/2-5 落地).
本战役 4-2 跟 VCP 无关, 9 器官 = Apeireth 哲学层原创.

---

## 边界确认

| 边界 | 状态 |
|------|------|
| 不动 R11 LOCKED / v6 / Cargo.lock | ✅ 0 触碰 |
| 不动战役 1 + 战役 2 + 战役 4-1 代码 | ✅ 0 触碰 (用, 不改) |
| 不假装: 9 器官 health 真算, 不写死 | ✅ 9 器官全部真后端, 12 tests 验证 |
| 不抄 VCP 业务代码 | ✅ 9 器官 = Apeireth 原创 |
| 改 `crates/apeireth-tui/src/{Cargo.toml,backend.rs}` + 加 unit tests + commit | ✅ backend.rs +535/-23, 0 改 Cargo.toml (依赖早已齐), 12 tests, commit `719377b8` |

---

## 验证清单 (DoD)

- ✅ **9 器官 health + primary + secondary + tertiary 全部从真后端算**:
  - perception: 0.00 / "0/5 通道" / "Text · Voice · Vision" / "events: 0"
  - cognition: 1.00 / "V0.5=1.000" / "1 verdicts · allowed=true" / "cycle 0"
  - consciousness: 0.33 / "awake" / "awake / self_disabling" / "machine_id=apeireth-tui-default"
  - memory: 0.00 / "0 episodes (total)" / "web: 0 · council: 0" / "identity: ✗"
  - motivation: 0.91 / "0.908" / "✓ 过门槛 (≥0.85)" / "SGI 7 约束 (C-SGI-1~7)"
  - value: 0.50 / "5/5 层洋葱" / "E / S / A / M / O" / "— SGI 未设"
  - relation: 0.25 / "1/4 类关系" / "共生 / 协调 / 嵌入 / 与自身" / "continuity: apeireth-tui-default"
  - action: 0.50 / "3 模式" / "Execute / Express / Silence" / "cycle 0 (越用越熟)"
  - life_force: 1.00 / "1.000" / "dormant" / "SGI: (空)"

- ✅ **端到端真测 (TUI 跑起, bridge 9 器官 3x3 网格真从后端拉)**: release exe 跑 3 秒抓 stdout, 9 器官 health + primary 全部显示真后端值

---

## 下一步候选 (W4 留给主人定夺)

1. **空数据兜底** — 当前默认 MEMORY_STORE 空 → 9 器官 health 普遍偏低 (0.00-0.91). W4+ 可考虑:
   - 注入 demo data (apeireth-memory `seed` 子命令) 让首次跑也有可看 health
   - 或 status bar 显示 "首次跑, episode=0, 健康度低" 提示
2. **CYCLE_COUNT 累加快照** — `snapshot_action` 的 health 跟 cycle_count 强相关, 但 chat() 调 run_cycle 才 +1. W4+ 可考虑对话 page 自动 inject 几次 cycle
3. **perception 真后端深化** — 当前 active = distinct session_id 数, 简单但不够"激活". W4+ 可引入 `apeireth-perception` 的 process_all/pipeline 真跑
4. **value 真后端深化** — 当前 health = SGI 二元 (有/无). W4+ 可用 ValueCandidate.motivation_score() 真算子分

---

## 工程铁律自检 (主 17:43 实事求是 + 主 17:58 不假装)

| 铁律 | 状态 |
|------|------|
| 不假装 | ✅ 9 器官 health 全部真算, 0 hardcode (验证 grep), 12 tests 断言 primary 字段来自真后端 |
| 编译期 hardcode | ✅ ChannelKind 5 / CognitiveDreamState 6 / ValueDimension 5 / RelationKind 4 / 3 trait 全部编译期 const |
| 不漂移 | ✅ backend.rs 1 file +535/-23, 不动其他文件 |
| 不绕过 V1+V2+V3 AND 门 | ✅ 0 触碰 run_cycle / decision / verdict |
| 不绕过 Self-Disable 5 大机制 | ✅ 0 触碰 SelfDisableGuard |
| 不绕过 4 重守门 | ✅ 0 触碰洋葱 |

---

**结语**: TUI 战役 4-2 跑通, 9 器官 3x3 网格 100% 真后端, 12 tests 全绿, 端到端验证 release exe 真显示真后端值 (不是 W1 9 个固定 0.85/0.92/0.95/0.78/0.88/0.90/0.83/0.86/0.96), 符合主人 5 项不假装 + 8 项不修改承诺 + 主哲学 6 锚穿透。下一步 W4 待主人定夺。
