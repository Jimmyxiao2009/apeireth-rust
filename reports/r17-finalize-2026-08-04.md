# R17 Finalize — apeireth-api 真自研直连 LLM 双协议

**日期**: 2026-08-04
**作者**: 楚零 (按主人 2026-08-03 22:44 授权, OpenClaw session 沿用 chuling 命名)
**路径**: `.openclaw\workspace\promethean\Apeireth-rust\`
**HEAD**: 35e56243 (R17-conventions) ←  R17-drift f68c8faf ← round17-07 be561705

---

## 📌 R17 阶段定位

R17 = "R16 完成后的 **一次性大改**",核心目标:**apeireth-api 砍掉 NewAPI 借鉴,真自研直连 minimaxi 的 OpenAI + Anthropic 双协议**。

主语调整:
- ❌ R16: "apeireth-api 是 NewAPI 风格的通用 LLM 平台"(借鉴 channel 路由)
- ✅ R17: "apeireth-api 是 **自研 + 嵌入** 的协议适配层,直连 Anthropic Messages API + OpenAI Chat Completion API,minimaxi 只是已验证的具体 provider"

```
R11/R12/R13/R14  ──  Rust 重写 + 多阶段 + 24 维测量 + 5 重治理 + 7 advisor (基础工程)
R15              ──  5 器官落地 (perception/cognition/action/motivation/value)
R16              ──  apeireth-llm + apeireth-api HTTP 平台 + Council 真 LLM 接入
R17  ⭐          ──  砍 NewAPI 借鉴,真自研双协议直连,真 API key 端到端跑通
R18+             ──  前端 (Leptos SSR + WASM hydration) + 6 类非 LLM API 调研
```

---

## ✅ 完工内容总览 (7 commit)

| # | commit | 主题 | 状态 |
|---|--------|------|------|
| R17-01 | `e017e8e9 round17-01 (chuling via mavis)` | 砍 NewAPI 依赖 + 重写 LLM 入口 | ✅ 真 minimaxi /v1 接通 (6834ms, 352 tokens) |
| R17-02 | `a414700d round17-02 (chuling via mavis)` | 加 `AnthropicCompatibleProvider` 走 minimaxi `/anthropic` | ✅ 真 minimaxi /anthropic 接通 (2217ms, 96 tokens, end_turn) |
| R17-03 | `99172916 round17-03 (chuling via mavis)` | 砍 `src/gateway/` 借鉴 NewAPI channel 路由 | ✅ 测试 1707→1675 (-32,符合预期) |
| R17-06 | `53996598 round17-06 (chuling via mavis)` | 端到端真效果验证-记忆 (apeireth-memory) | ✅ 6 episode 真持久化 + drop + 重建 |
| R17-07 | `be561705 round17-07 (chuling via mavis)` | 端到端真效果验证-权限 (apeireth-sovereignty) | ✅ 5 大 Self-Disable 全真触发 + records append-only |
| R17-08 | `f68c8faf R17-drift` | 命名规范 v12 升级 drift 报告 | ✅ |
| R17-08 | `35e56243 R17-conventions` | APEIRETH-CONVENTIONS v12 升级 (增量) | ✅ |

---

## 🔑 真实 API key 验证数据 (基于 minimaxi)

| 验证 | 端点 | latency | tokens | result |
|------|------|---------|--------|--------|
| **R17-01 `hello_api`** | `https://api.minimaxi.com/v1` (OpenAI 协议) | **6834ms** | 352 (prompt + completion) | ✅ pass |
| **R17-02 `anthropic_hello`** | `https://api.minimaxi.com/anthropic` (Anthropic 协议) | **2217ms** | 96 | ✅ pass, finish: end_turn |
| **R17-06 memory 持久化** | apeireth-memory episode store | local | 6 episode 写入→drop→重建 6 episode 全在 | ✅ 真持久化 |
| **R17-07 权限 5 大机制** | apeireth-sovereignty self-disable | local | NoDegrade/NoPatch/NoBypass/NoReverse/NoHide 全真触发 | ✅ 全部识别 |

**所有验证基于主人 2026-08-03 22:13 "你自己读 apikey 文档, 把 apikey 真接入 Apeireth" 的真实授权。**

---

## 🏗️ 架构调整 (R16 → R17)

### 砍掉 (借鉴 NewAPI 的部分)
```
❌ src/admin.rs                         (NewAPIAdminClient 借鉴)
❌ src/http.rs                          (借用 NewAPI 的 HTTP 类型)
❌ src/gateway/channel.rs               (8 ChannelType 借鉴)
❌ src/gateway/config.rs                (TOML channel 配置)
❌ src/gateway/error.rs                 (GatewayError)
❌ src/gateway/manager.rs               (ChannelManager 状态机)
❌ src/gateway/mod.rs                   (gateway 模块)
❌ src/gateway/router.rs                (通道路由)
❌ examples/admin_demo.rs               (admin demo)
❌ examples/gateway_demo.rs             (gateway demo)
❌ crates/apeireth-cli/examples/real_effect_demo.rs (R16 失败 example, 18 编译错误)
```

### 新增 / 重写 (真自研)
```
✅ src/llm/providers/apeireth_api.rs   (重写,默认 base_url → https://api.minimaxi.com/v1)
✅ src/llm/providers/anthropic_compat.rs (新增,480 行, 6 unit test)
   - x-api-key header (不是 Authorization: Bearer)
   - system 字段从 messages 拆出来
   - finish_reason: end_turn (Anthropic 协议) vs stop (OpenAI 协议)
✅ examples/hello_api.rs                (注释更新)
✅ examples/anthropic_hello.rs          (新增,Anthropic 协议演示)
✅ examples/memory_effect_demo.rs       (apeireth-memory 端到端)
✅ examples/permission_effect_demo.rs   (apeireth-sovereignty 端到端)
```

### 简化
```
✅ src/server.rs                        (AppState 只保留 llm 字段,删 /channels endpoint)
✅ examples/serve.rs                    (跟着简化)
✅ crates/apeireth-cli/src/lib.rs + main.rs (GatewaySubCommand 只留 Serve)
```

---

## 📊 数字

| 维度 | R16 → R17 变化 |
|------|---------------|
| 真实 API key 验证 | 5 endpoint → 2 协议 (OpenAI + Anthropic) |
| 测试数 | 1707 → 1675 (-32,删 gateway 测试) |
| 借鉴 NewAPI 文件 | 11 个 → 0 个 (全砍) |
| 新增自研文件 | 0 → 2 (anthropic_compat.rs + 3 个 demo) |
| 真 LLM 接通协议 | 1 (OpenAI) → 2 (OpenAI + Anthropic) |
| 真 LLM 端到端验证 | hello_api + 5 endpoint → hello_api + anthropic_hello + memory + permission 4 个 example |
| 文档 | 7 份 R16 报告 + 1 份 R17 drift → 5 份 R17 报告 (本报告 + 4 个 week) |

---

## 🎯 核心不破坏承诺 (R17 守住了什么)

✅ **Cargo.toml `version = "0.14.0"` 不变**
✅ **R11 baseline 三值 (V1141=0.8682 / V1131=0.8532 / V1136=0.9063) LOCKED**
✅ **Document-Meta 格式不变 (v12 只升 version 字段: Manual-Rev-H + Fix-12)**
✅ **阶段 1+2+3+4+5 LOCKED 内容不动**
✅ **v1-v10 修正链文件名保留**
✅ **12 子规范系统结构不变 (v12 只在 §1 / §5 / §6 加 R15+ 实践)**
✅ **7 项不修改承诺 不变**
✅ **apeireth-llm / apeireth-api crate 名字保留** (用户明确要求)

apeireth-api 内部从 "NewAPI 风格的通用 LLM 平台" 重写为 "自研双协议直连层" 属于**实现重写,接口/名字保留**,不破坏承诺。

---

## 🛤️ 路径 (R17 → R18+)

### R17 副线讨论沉淀 (2026-08-03 23:33 后)

**Rust 前端选型**:用户倾向 **Leptos**(同 axum 社区作者,SSR + WASM hydration,真正"配套高效")
- ✅ 同生态优势: axum (后端) + Leptos (前端) 同一作者 Greg Johnston,共享服务生态
- ✅ SSR 优势: 后端 Council 7 advisor 辩论结果直接 SSR 渲染,前端只做 hydration
- ✅ WASM hydration: 客户端继续交互,后端结果渐进增强
- ⚠️ 风险: Leptos 0.7+ 还在快速演进,文档相对新

**派 sub-agent 写前端 MVP**:
- ✅ 适合: 独立 example、独立验证 (例如 Council 7 advisor UI mock)
- ❌ 不适合: 架构讨论、核心 trait 改动 (例如要不要把 Council trait 拆异步)

### R18+ 计划 (待用户拍时机)

1. **R18 前端 MVP (Leptos SSR + WASM hydration)**
   - Council 7 advisor 辩论 UI 设计
   - V1+V2+V3 决策面板
   - Memory episode 时间线
   - 派 sub-agent 写独立组件
2. **R19+ 6 类非 LLM API 调研 (用户: "太难不做也行")**
   - 视频 / 语音 / 音色 / 图像 / 音乐 / 文件
3. **R20+ Apeireth 商业化路径**
   - 自部署 / SaaS / 嵌入式 SDK 三选一
   - 用户场景: 2026 年学术研究项目

---

## 🧪 主人验收步骤 (R17 全栈)

```powershell
# 1. 设置真实 API key
$env:APEIRETH_API_KEY = (Get-Content .minimax-agent-cn\projects\apikey.txt)[0].Trim()

# 2. 跑双协议 demo
cd .openclaw\workspace\promethean\Apeireth-rust
cargo run -p apeireth-api --example hello_api          # OpenAI 协议
cargo run -p apeireth-api --example anthropic_hello    # Anthropic 协议

# 3. 跑端到端真效果验证
cargo run -p apeireth-memory --example memory_effect_demo
cargo run -p apeireth-sovereignty --example permission_effect_demo

# 4. 看测试 (1675 passed / 0 failed)
cargo test --workspace

# 期望输出：
# ✅ hello_api: 200 OK minimaxi /v1 (6834ms, 352 tokens)
# ✅ anthropic_hello: 200 OK minimaxi /anthropic (2217ms, 96 tokens, end_turn)
# ✅ memory_effect_demo: 6 episode 写入→drop→重建 全在
# ✅ permission_effect_demo: 5 大 Self-Disable 全真触发
```

---

## 📂 报告索引 (R17 全部按 v12 新规范)

| 报告 | 路径 |
|------|------|
| R17 收尾 (本报告) | `reports/r17-finalize-2026-08-04.md` |
| R17-01 砍 NewAPI 依赖 | `reports/r17-week1-llm-direct-minimaxi-2026-08-04.md` |
| R17-02 Anthropic 协议 | `reports/r17-week2-anthropic-protocol-2026-08-04.md` |
| R17-03 砍 gateway/ | `reports/r17-week3-gateway-cleanup-2026-08-04.md` |
| R17-06/07 端到端真效果 | `reports/r17-week4-effect-verification-2026-08-04.md` |
| R17 drift 报告 | `reports/drift-stage7-r17-naming-v12-promotion-2026-08-04.md` |
| APEIRETH-CONVENTIONS v12 | `APEIRETH-CONVENTIONS.md` (Manual-Rev-H + Fix-12) |

---

## ✅ 验收结论

**R17 主体完工,真 minimaxi 双协议全通,真端到端效果验证全过**。

- ✅ apeireth-api 真自研 + 嵌入 + 直连 OpenAI + Anthropic 双协议
- ✅ 真实 API key 接通 minimaxi 双端点 (6834ms / 2217ms)
- ✅ 端到端记忆/权限真效果验证 (memory 6 episode 持久化, sovereignty 5 Self-Disable 全触发)
- ✅ 砍掉 11 个借鉴 NewAPI 文件,新增 2 个自研协议适配
- ✅ 1675 测试全过,8 项不修改承诺全守
- ✅ APEIRETH-CONVENTIONS v12 升级 (命名规范补 R15+ 实践)

**前端进 Leptos,后端补 6 类非 LLM API (用户拍时机),R17 阶段收工。**

---

**作者**: 楚零 (按主人 2026-08-03 22:44 授权 R17 一次性大改, OpenClaw session 沿用 chuling 命名)
**R17 累计**: 7 commit (R17-01 / 02 / 03 / 06 / 07 / drift / conventions) + 1 收尾报告
**下次开工**: 主人拍 R18 时机 (前端 MVP 选 Leptos 启动)
