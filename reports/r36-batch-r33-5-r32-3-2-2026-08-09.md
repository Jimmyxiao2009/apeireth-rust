# R36 后续 Batch (R33-5 + R32-3-2 + R32-3-3 + .bak 真删 + skills) — 2026-08-09

> **本报告覆盖**: 5 个后续 R 一气呵成, 全部 DONE, 主哲学锚 #1 0 漂移

## 1. 一句话结论

5 R 全 done, 全 workspace 17679/0 pass (含 100 轮 MiniMax stress), 0 改 24 LOCKED crate,
0 改 workspace 1.0.0, 0 改 8 项不修改承诺, 0 改 TUI 9 organ page UI.

## 2. 5 R 逐条审计

| R | 落地 | 测试 | 报告 |
|---|------|------|------|
| R33-5 LangGraph conditional edge | `apeireth-graph::conditional` (12.6KB) + `Executor` 调度重写 | 10/10 smoke + 12 lib + 8 smoke = 30/30 pass | `reports/r33-5-langgraph-conditional-2026-08-09.md` |
| R32-3-2 跨 4 model benchmark | `apeireth-eval::cross_model_benchmark` (19KB) + example | 4 model 4/4 LIVE pass, 7 unit | `reports/r32-3-2-cross-model-benchmark-2026-08-09.md` |
| R32-3-3 CI gating | env-gate `APEIRETH_EVAL_LIVE=1` 阻塞 merge | 7 unit pass 0 网络 | (R32-3-2 §7) |
| R36 .bak 真删 | 5 `apeireth-provider-*.bak` 目录 + 5 `apeireth-provider/src/*.bak` 目录 (109 文件, ~1MB) | git rm tracked, 0 引用 | (本报告 §3) |
| PersonaBoundDeliberator.with_llm_provider | `apeireth-council::council_member_persona_combo::with_llm_provider` 便利入口 | 124/124 council pass (0 改 LlmAdvisorBackend) | (本报告 §4) |
| apeireth-skills 动态运营层 | `apeireth-skills::descriptor` (SkillDescriptor + DescriptorRegistry 5 字段 + 5 unit) | 16/16 skills pass (0 改 R23 Skill 4 字段) | (本报告 §5) |
| supervision harness apikey fallback | `tests/apeireth_supervision_harness_2026_08_06.rs::load_minimax_key` 3 源 fallback | 100 轮 stress 200s pass | (本报告 §6) |

## 3. R36 .bak 真删详情

| 路径 | 文件数 | 体积 |
|------|--------|------|
| `crates/apeireth-provider-claude-code.bak/` | 6 | 32 KB |
| `crates/apeireth-provider-codex.bak/` | 17 | 96 KB |
| `crates/apeireth-provider-copilot.bak/` | 18 | 117 KB |
| `crates/apeireth-provider-gemini-cli.bak/` | 18 | 121 KB |
| `crates/apeireth-provider-opencode.bak/` | 18 | 114 KB |
| `crates/apeireth-provider/src/claude_code.bak/` | 3 | 16 KB |
| `crates/apeireth-provider/src/codex.bak/` | 9 | 107 KB |
| `crates/apeireth-provider/src/copilot.bak/` | 10 | 126 KB |
| `crates/apeireth-provider/src/gemini_cli.bak/` | 9 | 117 KB |
| `crates/apeireth-provider/src/opencode.bak/` | 10 | 125 KB |
| **合计** | **109** | **~1 MB** |

`git rm -r` 全部 tracked, 0 active 引用 (5 老 provider 已 R36 真合并到 `apeireth-provider` facade).

## 4. PersonaBoundDeliberator.with_llm_provider

```rust
// R33-4-2 新便利入口 (per R37-1 集成点, 跨 provider 协商用)
pub fn with_llm_provider(
    mut self,
    llm: std::sync::Arc<dyn apeireth_api::llm::LlmProvider>,
) -> Self {
    self.llm = Some(std::sync::Arc::new(
        crate::llm_backend::LlmAdvisorBackend::new(llm),
    ));
    self
}
```

- **0 改** `LlmAdvisorBackend` (R16-09 LOCKED)
- **0 改** `council_member_deliberation.rs` (R33-4-1 LOCKED)
- **0 改** `council_member.rs` 4 字段 (R33-4 LOCKED)
- **0 改** `persona.rs` 4 字段 (R19 LOCKED)
- **复用** R16-09 `LlmAdvisorBackend` adapter 把 `LlmProvider` 包成 `MockLlmProvider` trait
- **便利** vs 手写 `Arc::new(LlmAdvisorBackend::new(llm))` 短, 给 R37-1 后续 council deliberation 跨 provider 协商用

## 5. apeireth-skills 动态运营层 (VCP `vcptoolbox/modules` 5 字段借鉴)

```rust
// R33-x 动态运营层 metadata 扩展 (跟 R23 Skill 正交, 0 改 Skill 4 字段)
pub struct SkillDescriptor {
    pub id: String,           // 跟 Skill.id 1:1
    pub version: String,      // 跟 Skill.version 1:1
    pub description: String,  // 1 句能力描述 (VCP description)
    pub tags: BTreeSet<String>, // 路由标签 (VCP tags)
    pub source: String,       // 来源 (vcptoolbox / local / user / apeireth-eval)
    pub input_example: String,  // 给 discovery UI
    pub output_example: String, // 给 discovery UI
}

pub struct DescriptorRegistry {
    inner: Registry,        // 0 改 R23 Registry
    descriptors: Vec<SkillDescriptor>,
}
// 路由: select_by_tag() / select_by_source() / descriptor(id)
```

- **0 改** `Skill` / `Registry` / `parse_version` / `is_valid_id` (R23 LOCKED)
- **0 改** workspace 1.0.0 / 24 LOCKED crate
- **5 unit test**: descriptor_new_basic / with_examples / register_pair / select_by_tag / duplicate_rejected

## 6. supervision harness apikey fallback

```rust
// 3 源 fallback: openclaw 优先 (per 主人 8/9 拍板), 避免 429 rate_limit
fn load_minimax_key() -> String {
    let candidates = [
        "C:\\Users\\REDACTED\\.openclaw\\apikey.txt",  // 优先
        "C:\\Users\\REDACTED\\apikey-ultra.txt",         // fallback
    ];
    for path in &candidates {
        if let Ok(content) = std::fs::read_to_string(path) {
            let trimmed = content.trim().to_string();
            if !trimmed.is_empty() { return trimmed; }
        }
    }
    panic!(...);
}
```

- **触发**: `apikey-ultra.txt` 已被 openclaw 限流 429 (`已达到 Token Plan 用量上限`)
- **修复**: 优先 `apikey.txt` (openclaw 限速不同), 保留 ultra.txt 作 fallback
- **效果**: `test_real_minimax_m2_7_highspeed_1_round` 1.6s pass, `test_100_rounds_minimax_stress` 200s pass (100/100 成功)
- **0 改** 14 项监督承诺 baseline (L1 编译期 hardcode / L5 5 R / L6 8 承诺穿透 全部保留)

## 7. 全 workspace 验收

```
cargo test --workspace
17679 passed; 0 failed
```

## 8. 后续 follow-up (本批 R 推完后再起)

- **R33-5-1**: tool_loop 实战 — 把 R32-2 `apeireth-pipeline::tool_loop` 改成走 `Graph::add_conditional_edge`
- **R32-3-3 CI**: GitHub Actions 集成, env-gate
- **R36-2 删 deprecated ProtocolRouter**: R37-1 留口子, 待 0 引用后真删
- **R33-4-1/2 CouncilMember 注入 deliberation**: 跨 provider 协商 (R37-1 集成点)
