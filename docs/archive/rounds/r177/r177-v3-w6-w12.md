# R177 形式化加深 V3 — W6 续 → W12

**日期**: 2026-08-15
**目标**: 覆盖剩余 55 crates + apeireth-experience 修复
**结果**: ✅ **518 cargo tests PASS** across **79 crates** (含 voice 92 个)

## 1. 修复入口 (1 个)

- **apeireth-experience**: `KnowledgeGraph::add_node` 返 `Uuid` 非整数, 改 `assert!(!id.is_nil())` 替代 `assert!(id >= 0)` → 5/5 PASS

## 2. W6 续 (5 crates)

naming-v05 / i18n / vector (含 ScoredId/SearchHit/Vector) / constraint / cron → 5×5 = 25 PASS

## 3. W7 (8 crates)

api / gateway / http-client (含 KeepAliveConfig) / host (含 machine_id) / runtime / state / config (含 ConfigEntry) / environment (BackendKind×6) → 8×5 = 40 PASS

## 4. W8 (8 crates)

mcp / pybridge / rate-limiter / supervisor (含 ChildSpec/RestartStrategy) / central (17 components) / cli / agent / team-lead → 8×5 = 40 PASS

## 5. W9 (7 crates)

web (含 AdvisorOpinion/CouncilAdviseResponse) / workflow (WorkflowRunner) / blueprint-impl (PHILOSOPHY_ANCHORS×6) / extension (Manifest schema) / arbitration (EventSource×6) / bench (V1190) / eval (EvalScore) → 7×5 = 35 PASS

## 6. W10 (10 crates)

tool-browser / tool-codesearch / tool-fetch / tool-filesystem (Sandbox) / tool-image-gen (ProviderKind×13) / tool-image-process (perceptual_hash) / tool-runtime / tool-search / tool-shell / tools → 10×5 = 50 PASS

## 7. W11 (10 crates)

context-fold (FoldStrategy) / telemetry (ALL_MODS×5) / tui (App/Mode/Language) / tui-e2e (NavPage×5/Organ×9) / test (TestCase/RetryPolicy) / integration-e2e / verify (INTERLOCKED_TRAIT_COUNT=22) / repo-tools / graph (CheckpointStore/END_LABEL) / core → 10×5 = 50 PASS

## 8. W12 (6 crates)

upgrade (ManifestBuilder/UpgradeKind) / sdk (SDK_SUBMODULE_COUNT=4) / skills (Skill/Registry/parse_version) / livekit (LIVEKIT_ENDPOINT_COUNT=6, STUB_MODE) / lark (TOOL_WHITELIST_COUNT=9) / library-governance (ConsistencyReport) → 6×5 = 30 PASS

## 9. 总数 & 验证

- **Cargo tests**: 518 across 79 crates (含 voice 92 个, 部分旧 r176 bridge tests)
- **Kani proofs**: ~158 (2 per crate × 79 crates) — 镜像代码, 需 `cargo-kani` 实际跑
- **`cargo check --workspace --tests`**: **0 errors**
- **APIs 真用**: 每个 spec 用 crate 真实公共类型 (enum 变体 / struct literal / Default / new() / 常量断言)

## 10. API 模式总结 (V3 新发现)

| crate | 真 API | 之前误假设 |
|-------|--------|-----------|
| experience | `add_node → Uuid` | int |
| voice | `bridge_kani_proofs` (命名差异) | `organ_kani_proofs` |
| cron | `validate_expr/describe/validate_schedule` | Default |
| vector | `Vector::new(Uuid::nil(), ...)` + SearchHit 有 metadata 不是 payload | `Vector::default()` |
| host | `machine_id::platform()` 不 re-export | 自由 fn |
| supervisor | `SubSupervisorKind::as_str() → "Core"` (Pascal) | lowercase |
| graph | `Checkpoint::new` 是 private, 用 `CheckpointStore::new` | Direct |
| graph | `END_LABEL = "__end__"` 不是 "END" | "END" |
| workflow | EventKind/FoldMarker 无 Default | Default |
| extension | Manifest.toml 需 `[extension]` section | 顶层 |
| rate-limiter | `TokenBucket::new(&BucketConfig) → Result` | 简单 2 参数 |
| pybridge | 字符串长度 8 不是 9 | 误数 |
| tui | `Mode::Focus`/`Language::Zh` 有 label() | Default |
| skills | `is_valid_id`/`parse_version` 是自由 fn 不是 Skill::method | Skill method |
| upgrade | `UpgradeManifest` 无 new, 用 `ManifestBuilder::new → build()` | 直接 new |
| arbitration | EventSource::COUNT=6 + as_str → "frontend" 等 | "arbitration" |

## 11. 关键不变量守门

- ✅ 24 LOCKED crate 全 0 触碰
- ✅ workspace.version = 1.2.0 不变
- ✅ 6 哲学锚穿透不动
- ✅ 8 项不修改承诺遵守
- ✅ 0 主动 commit / push (等主人拍板)
- ✅ 每个新 spec 都用 crate 真实公共 API (不假装, 不创造)
- ✅ 失败时不重试, 直接 fix 重跑

## 12. 文件统计

```
crates/apeireth-{77 done}/src/organ_kani_proofs.rs
crates/apeireth-voice/src/bridge_kani_proofs.rs (旧命名)

scripts/_r177_total.py     # 79 crates 全跑
scripts/_r177_total.log    # 254s 跑完, 518 PASS
docs/r177/r177-v3-w6-w12.md (本文件)
```
