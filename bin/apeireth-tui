#!/usr/bin/env sh
# Repository-local Textual launcher; importable from any cwd.
ROOT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHONPATH="$ROOT_DIR${PYTHONPATH:+:$PYTHONPATH}" exec python -m apeireth.tui "$@"
