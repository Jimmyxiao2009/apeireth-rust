# apeireth-runtime

> Apeireth R147: end-to-end runtime orchestration - HeartbeatScheduler + AsyncTaskStore + ChanneledBus + ArbitrationLog + SearchEngine + GroupChat + EmotionEngine integrated runtime. Borrows 'self-driven living day' concept (origin: open-source), lifted to Rust compile-time guaranteed unified runtime.

apeireth-runtime 是 Apeireth 1.0 (AGI 操作系统) 工作区 crate 之一。完整架构见 [docs/](../../docs/README.md)。
