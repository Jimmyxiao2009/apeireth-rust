# apeireth-llm-iface

> R179 P0-3: LLM 抽象接口 (ChatMessage / LlmRequest / LlmProvider / LlmError). 拆 apeireth-memory <-> apeireth-api 编译期边.

apeireth-llm-iface 是 Apeireth 1.0 (AGI 操作系统) 工作区 crate 之一。完整架构见 [docs/](../../docs/README.md)。
