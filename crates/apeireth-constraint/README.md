# apeireth-constraint

> Apeireth 约束器官 (P12 — v4.1 + R128 后: 13 键 verdict cache 复用 (含 PHL-07, 复用 apeireth-core ALL_TWELVE_KEYS + R128 PHL-07) + 4 重守门 FourGates trait (编译时/运行时/物理隔离/反思期, v15 命名修正) + PermissionGrant 三方授权 (Council 7 强制 + Human L0 HA + RiskLevel); FiveGates 保留为 deprecated 向后兼容别名)

apeireth-constraint 是 Apeireth 1.0 (AGI 操作系统) 工作区 crate 之一。完整架构见 [docs/](../../docs/README.md)。
