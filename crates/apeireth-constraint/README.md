# apeireth-constraint

> Apeireth 约束器官 (P12 — v4.1 新增: 12 键 verdict cache 复用 + 5 重守门 trait (编译时/运行时/多AI/物理隔离/反思期))

apeireth-constraint 是 Apeireth 1.0 (AGI 操作系统) 工作区 crate 之一。完整架构见 [docs/](../../docs/README.md)。
