# apeireth-tool-filesystem

> Apeireth R137: filesystem extension (sandbox + atomic write + fsnotify + file lock + doc parsing)

apeireth-tool-filesystem 是 Apeireth 1.0 (AGI 操作系统) 工作区 crate 之一。完整架构见 [docs/](../../docs/README.md)。
