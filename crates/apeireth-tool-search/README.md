# apeireth-tool-search

> Apeireth R145 VSearch: 全文 + 聚合 + TF-IDF 排序内存搜索. 区别于 apeireth-tool-codesearch (regex/AST) + apeireth-tool-image-process (perceptual). 上升为 Rust 编译期保证, 字段级复刻 VSearch/VSearch+ (origin: open-source).

apeireth-tool-search 是 Apeireth 1.0 (AGI 操作系统) 工作区 crate 之一。完整架构见 [docs/](../../docs/README.md)。
