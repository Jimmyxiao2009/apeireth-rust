[package]
name = "apeireth-test"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "apeireth-test — TestCase + RetryPolicy (exponential backoff) + SuiteSummary + Budget + flaky_cases (R23 P1 #5 实质化). 实质 1 模块 (lib.rs). R150 P1 #12 加 proptest dev-dep. 0 改 workspace.version."
[lib]
name = "apeireth_test"
path = "src/lib.rs"
[dependencies]
serde = { workspace = true }
[lints]
workspace = true

[dev-dependencies]
# R150 P1 #12: property-based testing (借鉴 alt-proptest/proptest 1.7K stars)
# 0 触碰 LOCKED (R148 已撤销), 仅 dev-dep
proptest = { workspace = true }
