# apeireth-tool-browser

> Apeireth R139: browser tool extension (Playwright accessibility tree + CLI/SKILL + MCP dual mode), HTTP fetch by default, optional CDP via chromiumoxide

apeireth-tool-browser 是 Apeireth 1.0 (AGI 操作系统) 工作区 crate 之一。完整架构见 [docs/](../../docs/README.md)。
