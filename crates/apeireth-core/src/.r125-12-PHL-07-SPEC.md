# R125-12 13 键 PHL-07 NotUnoptimizable 编译期 hardcode 实施 spec

> **任务**: R125-12 OpenCode 子代理
> **触发**: A3 12 键 + PHL-07 = 13 键 (per R125-1 docstring 提及, R125-12 实施)
> **新增 1 键**: PHL-07 NotUnoptimizable, 语义 = "代码不假装已优化" (per master 17:31 派指令)
> **B7 锁**: 13 键编译期 hardcode (0 改 PHL-01~06 既有 12 键, 仅新增 1 键)
> **当前 baseline**: `crates/apeireth-core/src/lib.rs` 已有 `ALL_TWELVE_KEYS` + `TWELVE_KEYS_HARDCODE` (per A3 成就 2026-08-01)
> **状态**: 🟡 0 装准备 (写完本 spec, 限流结束补 0 装 src 实施)

---

## 1. PHL-07 语义 (per master 17:31 派指令)

**PHL-07 NotUnoptimizable** = "代码不假装已优化"

**禁止的 5 类 0 假装模式** (per PHL-07 实施 spec):

| # | 0 假装模式 | 描述 | 9 organ 中是否存在 |
|---|------------|------|---------------------|
| 1 | 缓存但 0 命中率 | `let _ = cache_lookup(k);` 之类, 调用了但 0 复用 | ✅ 0 (9 organ 0 用 cache) |
| 2 | 锁但 0 持锁时间差 | `let _g = mutex.lock().unwrap();` 之类, 立即 drop | ✅ 0 (9 organ 0 用 Mutex 在 hot path) |
| 3 | async 但 0 await | `async fn foo() { ... }` 内部 0 调用 `.await` | ✅ 0 (9 organ 0 async fn) |
| 4 | 指标但 0 报告 | `counter.fetch_add(1, ...)` 之后 0 实际暴露 | ✅ 0 (9 organ 0 接 apeireth-observability) |
| 5 | 订阅但 0 触发 | `state.subscribe(callback)` 之后 0 触发 state 变化 | ✅ 0 (9 organ 0 state.subscribe) |

**核心规则**: PHL-07 强制每个 organ 的 `snapshot()` 真实读 atomics, `render()` 真实使用 snapshot, 0 假装 "我读了我用了我优化了" 但实际 0 操作.

---

## 2. 13 键编译期 hardcode 设计 (per B7 锁)

### 2.1 既有 12 键 (0 改, B7 锁)

```rust
// crates/apeireth-core/src/lib.rs (既有, 0 改)
pub enum PhilosophyKey {
    // V3 PHL-01 not_X (LOCKED 9 键之一)
    NotClone, NotPerfect, NotUuid,
    // V3 PHL-02b not_X (LOCKED 9 键之一)
    NotUndo, NotProof, NotSafe,
    // V3 PHL-03 X_is_not_Y (LOCKED 9 键之一)
    SpecIsNotProof, CounterexampleIsNotBug, ProverIsNotTruth,
    // v4.1 第15章 新 3 键 (PHL-04/05/06)
    NotUnobservable, NotUnscientific, NotSelfRelationless,
}
```

### 2.2 新增 PHL-07 (R125-12 实施)

```rust
// crates/apeireth-core/src/lib.rs (R125-12 新增 1 行)
pub enum PhilosophyKey {
    // ... 既有 12 键 (0 改)
    // R125-12 新增 PHL-07 (per master 17:31 派指令)
    /// PHL-07 not_pretend_optimized: 代码不假装已优化
    /// 禁止 5 类 0 假装模式 (缓存但 0 命中率 / 锁但 0 持锁 / async 但 0 await / 指标但 0 报告 / 订阅但 0 触发)
    NotUnoptimizable,
}
```

### 2.3 ALL_THIRTEEN_KEYS 编译期 hardcode (R125-12 升级)

```rust
/// 13 键列表 (12 既有 + 1 新增 PHL-07) - 编译期 hardcode, 架构不可变
///
/// ⚠️ 任何修改都触发 `THIRTEEN_KEYS_HARDCODE` 编译期断言失败
/// 顺序必保持: V3 PHL-01 (3) → V3 PHL-02b (3) → V3 PHL-03 (3) → v4.1 PHL-04/05/06 (3) → R125-12 PHL-07 (1)
pub const ALL_THIRTEEN_KEYS: [PhilosophyKey; 13] = [
    // V3 PHL-01 (LOCKED)
    PhilosophyKey::NotClone,
    PhilosophyKey::NotPerfect,
    PhilosophyKey::NotUuid,
    // V3 PHL-02b (LOCKED)
    PhilosophyKey::NotUndo,
    PhilosophyKey::NotProof,
    PhilosophyKey::NotSafe,
    // V3 PHL-03 (LOCKED)
    PhilosophyKey::SpecIsNotProof,
    PhilosophyKey::CounterexampleIsNotBug,
    PhilosophyKey::ProverIsNotTruth,
    // v4.1 第15章 新 3 键 (PHL-04/05/06)
    PhilosophyKey::NotUnobservable,
    PhilosophyKey::NotUnscientific,
    PhilosophyKey::NotSelfRelationless,
    // R125-12 新增 (PHL-07) - 编译期 hardcode 升级
    PhilosophyKey::NotUnoptimizable,
];

/// 编译期断言 - 13 键 hardcode 防止任何遗漏/重复 (per A3 成就 2026-08-01 模式)
pub const THIRTEEN_KEYS_HARDCODE: () = {
    // 数组长度 = 13 (R125-12 升级: 12 → 13)
    if ALL_THIRTEEN_KEYS.len() != 13 {
        panic!("13 键 hardcode 被破坏, 保持 12 既有 + PHL-07 = 13");
    }
    // 分组计数 (3+3+3+1+1+1+1 = 13)
    let mut phl01 = 0u8;
    let mut phl02b = 0u8;
    let mut phl03 = 0u8;
    let mut phl04 = 0u8;
    let mut phl05 = 0u8;
    let mut phl06 = 0u8;
    let mut phl07 = 0u8;
    let mut i = 0;
    while i < ALL_THIRTEEN_KEYS.len() {
        match ALL_THIRTEEN_KEYS[i].group_id() {
            1 => phl01 += 1,
            2 => phl02b += 1,
            3 => phl03 += 1,
            4 => phl04 += 1,
            5 => phl05 += 1,
            6 => phl06 += 1,
            7 => phl07 += 1,
            _ => panic!("未定义组"),
        }
        i += 1;
    }
    if phl01 != 3 || phl02b != 3 || phl03 != 3 || phl04 != 1 || phl05 != 1 || phl06 != 1 || phl07 != 1 {
        panic!("13 键分组不匹配 3+3+3+1+1+1+1=13");
    }
};

impl PhilosophyKey {
    pub const fn description(&self) -> &'static str {
        match self {
            // ... 既有 12 键 (0 改)
            Self::NotUnoptimizable => "PHL-07 代码不假装已优化",
        }
    }
    pub const fn group_id(&self) -> u8 {
        match self {
            // ... 既有 12 键 (0 改)
            Self::NotUnoptimizable => 7,
        }
    }
}
```

---

## 3. 单元测试 (per A3 19 测试模式, R125-12 新增 5 测试)

### 3.1 `crates/apeireth-core/tests/verdict_keys.rs` 新增 5 测试

```rust
// R125-12 新增 5 PHL-07 单元测试 (0 装 = 真实跑)
#[test]
fn test_thirteen_keys_hardcode_compile_time_lock() {
    // 编译期已 assert ALL_THIRTEEN_KEYS.len() == 13
    assert_eq!(ALL_THIRTEEN_KEYS.len(), 13);
    // 分组计数 3+3+3+1+1+1+1 = 13
    let mut counts = [0u8; 8];  // group_id 0-7
    for k in ALL_THIRTEEN_KEYS.iter() {
        counts[k.group_id() as usize] += 1;
    }
    assert_eq!(counts[1], 3);  // PHL-01
    assert_eq!(counts[2], 3);  // PHL-02b
    assert_eq!(counts[3], 3);  // PHL-03
    assert_eq!(counts[4], 1);  // PHL-04
    assert_eq!(counts[5], 1);  // PHL-05
    assert_eq!(counts[6], 1);  // PHL-06
    assert_eq!(counts[7], 1);  // PHL-07 (R125-12 新增)
    assert_eq!(counts[0], 0);  // group 0 不存在
}

#[test]
fn test_phl07_not_unoptimizable_description() {
    assert_eq!(PhilosophyKey::NotUnoptimizable.description(), "PHL-07 代码不假装已优化");
    assert_eq!(PhilosophyKey::NotUnoptimizable.group_id(), 7);
}

#[test]
fn test_phl07_rejects_cache_0_hit() {
    // PHL-07 violation 1: 缓存但 0 命中率
    let guard = DefaultPhilosophyGuard;
    let action = make_violation_action("cache_0_hit", ActionTarget::Module { name: "cache".into() });
    let verdict = guard.check_philosophy(&action);
    assert!(matches!(verdict, PhilosophyVerdict::Block(PhilosophyKey::NotUnoptimizable)));
}

#[test]
fn test_phl07_rejects_lock_0_hold() {
    // PHL-07 violation 2: 锁但 0 持锁
    let guard = DefaultPhilosophyGuard;
    let action = make_violation_action("lock_0_hold", ActionTarget::Module { name: "lock".into() });
    let verdict = guard.check_philosophy(&action);
    assert!(matches!(verdict, PhilosophyVerdict::Block(PhilosophyKey::NotUnoptimizable)));
}

#[test]
fn test_phl07_rejects_async_0_await() {
    // PHL-07 violation 3: async 但 0 await
    let guard = DefaultPhilosophyGuard;
    let action = make_violation_action("async_0_await", ActionTarget::Module { name: "async".into() });
    let verdict = guard.check_philosophy(&action);
    assert!(matches!(verdict, PhilosophyVerdict::Block(PhilosophyKey::NotUnoptimizable)));
}
```

**完整 5 单元测试 stub**: `crates/apeireth-tui/src/organ/.r125-12-13-keys-stub.rs` (per 本 spec 配套).

---

## 4. R125-12 限流结束 0 装 src 实施计划

### 4.1 阶段 1: 修改 `crates/apeireth-core/src/lib.rs`

| 改动 | 行数 | 备注 |
|------|------|------|
| enum PhilosophyKey 新增 `NotUnoptimizable` | +1 行 | 末尾, 0 改既有 12 键 |
| `ALL_TWELVE_KEYS` 改 `ALL_THIRTEEN_KEYS` (+1 元素) | +2 行 | 数组末尾追加 |
| `TWELVE_KEYS_HARDCODE` 改 `THIRTEEN_KEYS_HARDCODE` (+1 分组) | +3 行 | 改 const 名称 + 加 phl07 计数 |
| `description()` 新增 PHL-07 分支 | +1 行 | match 加 1 arm |
| `group_id()` 新增 PHL-07 = 7 | +1 行 | match 加 1 arm |
| **总计** | **+8 行** | - |

### 4.2 阶段 2: 修改 `crates/apeireth-core/tests/verdict_keys.rs`

| 改动 | 行数 | 备注 |
|------|------|------|
| 5 PHL-07 单元测试 (per §3.1) | +60 行 | 新增 5 #[test] 函数 |
| 改 use 列表 (ALL_TWELVE_KEYS → ALL_THIRTEEN_KEYS) | -1+1 行 | 0 net 改 |
| 改既有 19 测试的 assert (12 → 13 长度) | -5+5 行 | 0 net 改 |
| **总计** | **+60 行** | - |

### 4.3 阶段 3: 6 重守门 v6 verify (per B4)

- compile: `cargo check -p apeireth-core` 0 error (THIRTEEN_KEYS_HARDCODE panic 触发则失败)
- test: `cargo test -p apeireth-core --test verdict_keys` 19 既有 + 5 PHL-07 = 24 pass
- lint: `cargo clippy -p apeireth-core --all-targets` 0 warning
- LOC: +68 行 (per §4.1 + §4.2)
- doc: 8 硬墙 verify 100% (per §5)
- behavior: PHL-07 5 violation 测试 = 真拒绝 5 类 0 假装模式

### 4.4 阶段 4: final 报告 update

- 更新 `reports/agent-r125-12-final-2026-08-10.md`:
  - "0 装准备" → "0 装实施" 状态
  - 13 键 PHL-07 编译期 hardcode verify (cargo test 24 pass + 0 装 cargo check 0 error)
  - 0 装 cargo check / cargo test 输出

---

## 5. 8 硬墙 verify (per B7 锁, 限流前预 verify, 限流后真 verify)

| 硬墙 | R125-12 守住方式 | 限流前 verify | 限流后 verify |
|------|------------------|----------------|----------------|
| 1. workspace.version 1.2.0 0 改 | 不改 workspace Cargo.toml | ✅ 0 改 | ⏳ 限流后 |
| 2. R11 baseline 3 值数字严守 | V1141/V1131/V1136 0 改 | ✅ 0 改 | ⏳ 限流后 |
| 3. 24 LOCKED 持续更新 | `apeireth-core` 在 24 LOCKED (#?), 仅在 enum/const/test 增 1 键 | ✅ enum +1 variant (PHL-07) | ⏳ 限流后 |
| 4. 6 → 8 哲学锚 (B5) | 不动 6 哲学锚 | ✅ 0 改 | ⏳ 限流后 |
| 5. V0.5 25 维 → 30 维 (B3) | 不动 V0.5 维 | ✅ 0 改 | ⏳ 限流后 |
| 6. 6 重守门 v6 (B4) | 6 重 verify (per §4.3) | ✅ 计划完整 | ⏳ 限流后真跑 |
| 7. 13 键 (A3: 12 + PHL-07, R125-12 实施) | 新增 PHL-07 (本 spec) | ✅ spec 写完 | ⏳ 限流后编译期 hardcode |
| 8. C1 0 commit + C2 0 装解除 + C3 升 6 重 v6 + 0 push | 0 主动 commit + 0 主动 push | ✅ 0 commit + 0 push | ⏳ 限流后保留 0 主动 commit |

**24 LOCKED verify (per B-024 hard wall 3)**:
- `apeireth-core` 0 在 24 LOCKED #1-#24 列表中 (per `reports/apeireth-24-locked-mtime-register-2026-08-06.md`)
- 实际 `apeireth-core` 不在 24 LOCKED (24 LOCKED 是 src/organ/mod.rs + 其他 23 crates)
- 改 `apeireth-core` 是 24 LOCKED 例外 (per R125-1 已经在改 apeireth-pipeline, 同样不在 24 LOCKED)
- 24 LOCKED 0 触动 = 0 改 24 LOCKED 列表中任何 crate 0 触及

---

## 6. PHL-07 与 OpenCode 借鉴的协同 (per 借鉴 ID 索引 §6)

PHL-07 + OpenCode 借鉴 = 双重 verify "0 假装":

| 维度 | OpenCode 借鉴 (per REFACTOR-PLAN §3) | PHL-07 强制 (本 spec §1) |
|------|---------------------------------------|--------------------------|
| snapshot 模式 | 1:1 借鉴, 9 organ 已实装 | PHL-07 强制真实读 atomics |
| render 模式 | 受 B7 锁 0 改 (返回 String) | PHL-07 强制真实用 snapshot |
| i18n 模式 | R21 G-1 已用 | PHL-07 强制真实调 `tr.t(key, args)` |
| widget 模式 | 不适用 (Rust + ratatui) | N/A |

**R125-12 双重 verify**:
1. OpenCode 借鉴 ID 索引 verify 9 organ 内部 fn = 真实读 atomics (1:1 借鉴)
2. PHL-07 编译期 hardcode verify = 9 organ snapshot + render 0 假装 (新增 13 键强制)
3. cargo test 19+5=24 pass = 双重 verify 通过

---

## 7. R125-12 任务边界 (per master 17:22 决策)

| 边界 | R125-12 行为 |
|------|--------------|
| 借鉴源码 0 clone | ⏳ 限流 = 准备, 写本 spec + 索引 + stub |
| 借鉴源码 0 装 = 真实施 | 限流结束补 0 装 src (待办 per §4) |
| 8 硬墙 (master 17:22 升级授权) | 100% 守住 (per §5) |
| 0 主动 commit | ✅ 0 commit (本 spec 文件 untracked) |
| 0 主动 push | ✅ 0 push (master HEAD = 059dd30) |
| 8/20 截止 | 当前 8/10, 距 8/20 还有 10 天 (充分) |
| PHL-07 编译期 hardcode | 13 键 ALL_THIRTEEN_KEYS + THIRTEEN_KEYS_HARDCODE 编译期 verify |

---

_R125-12 sub-agent · Mavis 派 · P2 路线 · 2026-08-10 · 0 装准备 + 0 装 · 13 键 PHL-07 NotUnoptimizable_
