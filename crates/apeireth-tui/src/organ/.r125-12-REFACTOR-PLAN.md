# R125-12 9 Organ 199KB → 120KB 重构计划 (-40% 减量)

> **任务**: R125-12 OpenCode 子代理
> **触发**: B7 9 organ 内部 fn 借 OpenCode + A3 12 键 + PHL-07 = 13 键
> **B7 实质保留**: 9 organ 文件名 + 入口签名 0 改, 内部 fn 可借 OpenCode
> **当前 baseline**: 9 organ + mod.rs = 10 文件, 109.6 KB (2026-08-10 17:50 实测)
> **目标**: 120 KB (master 17:22 directive 199 → 120, 实际已 109.6 < 120, 借鉴 OpenCode 模式继续优化内部 fn)
> **状态**: 🟡 0 装准备 (写完本 plan, 限流结束补 0 装 src 实施)

---

## 1. 当前基线 (实测 2026-08-10 17:50)

| # | 文件 | 当前字节 | 减量目标 | 减量后 | 借鉴 OpenCode 模式 |
|---|------|---------|---------|--------|---------------------|
| 1 | `mod.rs` | 12648 | -600B | 12048B | (0 借鉴, dispatcher 锁 24 LOCKED 8.1) |
| 2 | `body.rs` | 5384 | -200B | 5184B | `util/log.ts` 复用 helper |
| 3 | `brain.rs` | 11116 | -800B | 10316B | `agent/agent.ts` 抽 cycle_phase |
| 4 | `ear.rs` | 14711 | -700B | 14011B | `routes/chat.tsx` 抽 event_log |
| 5 | `eye.rs` | 11032 | -600B | 10432B | `context/*` 抽 keystroke_log |
| 6 | `hand.rs` | 15771 | -1200B | 14571B | `tool/read.ts` 抽 tool_dispatch |
| 7 | `heart.rs` | 7061 | -500B | 6561B | `util/log.ts` structured |
| 8 | `memory.rs` | 13084 | -800B | 12284B | `session/*.ts` 抽 episode_count |
| 9 | `mind.rs` | 9396 | -500B | 8896B | `bus/*` 抽 cognitive_phase |
| 10 | `voice.rs` | 11982 | -400B | 11582B | (0 借鉴, snapshot 重构) |
| **总计** | **10 文件** | **112185 (109.6 KB)** | **-6300B (-5.6%)** | **105885 (103.4 KB)** | - |

**结论**:
- 当前 109.6 KB < 120 KB 目标 → 已达标
- 借鉴 OpenCode 主要价值 = **内部 fn 标准化 + 复用** (非字节数)
- master 199 → 120 (-40%) 数字 = 预想上限, 实际 baseline 已 109.6, R125-12 重点 = 内部 fn 模式统一, 不是 199 → 120 的绝对值

---

## 2. B7 9 organ 文件名 + 入口签名 0 改 (per master 17:22 decision-33 + 17:30:34 commit 21aa85f3)

### 2.1 9 文件名保留 (0 改)

```text
crates/apeireth-tui/src/organ/
├── mod.rs        (dispatcher, 0 改)
├── body.rs       (保留)
├── brain.rs      (保留)
├── ear.rs        (保留)
├── eye.rs        (保留)
├── hand.rs       (保留)
├── heart.rs      (保留)
├── memory.rs     (保留)
├── mind.rs       (保留)
└── voice.rs      (保留)
```

### 2.2 9 入口签名保留 (0 改)

每个 organ 的 entry point (per `Organ::dispatch_render` 调用约定) = `pub fn render(area: Rect) -> String`:

```rust
// 9 文件全部保持这个签名 (B7 锁)
pub fn render(area: Rect) -> String {
    let _ = area;  // 目前 0 frame 绘制, 0 假装 frame
    // ... 内部 fn 可借 OpenCode 重构
    out
}
```

### 2.3 内部 fn 可借 OpenCode (B7 例外)

| 内部 fn 类型 | 当前 9 organ 现状 | OpenCode 借鉴目标 |
|--------------|------------------|--------------------|
| `snapshot()` | 各 organ 独立 (heart/brain/hand/...) | OpenCode `state.subscribe` 模式, 抽 trait `OrganSnapshot` |
| `format_*()` | 各 organ 独立辅助 fn | OpenCode `format()` helper 模式, 抽 `format_helpers` 模块 |
| `age_phrase()` / `human_readable()` | heart.rs 已有, 其他 organ 0 复用 | OpenCode `humanize.ts` 模式, 抽 `common::humanize` |
| `state atomics` | 各 organ 独立 `*_STATS` | OpenCode `atom.ts` 模式, 抽 `common::AtomicStats` trait |

---

## 3. OpenCode 借鉴的具体 4 类模式 (per 借鉴 ID 索引 §4)

### 3.1 模式 1: snapshot 模式 (1:1 借鉴, 0 改)

OpenCode 公开 doc 描述: 每个 widget 通过 `state.subscribe` 读 atomics, 不持锁.

**当前 9 organ 已 1:1 实现** (per heart.rs L41-43):
```rust
pub fn snapshot() -> HeartState {
    HeartState {
        beat_ticks: heart_stats::HEART_BEAT_TICKS.load(Ordering::Relaxed),
        last_tick_unix_ms: heart_stats::HEART_LAST_TICK_MS.load(Ordering::Relaxed),
        now_unix_ms: now_ms(),
        cycle_count: cycle_count_load(),
        r19_token_used: r19_token_used_load(),
    }
}
```

**借鉴动作**: 0 (已 1:1). 0 装, 仅 0 假装 verify.

### 3.2 模式 2: render_with_layout 模式 (0 借鉴)

OpenCode 公开 doc 描述: widget 用 Layout 切区域 + frame.draw.

**当前 9 organ**: 0 frame 绘制, 全部返回 `String` (per B7 锁 入口签名 `pub fn render(area: Rect) -> String`).

**借鉴动作**: 0 (受 B7 锁限制). 0 装, 0 假装 frame.

### 3.3 模式 3: i18n 异步 (0 借鉴, R21 G-1 已用)

OpenCode 公开 doc 描述: t(key, args) async 5 Locale.

**当前 9 organ**: R21 G-1 已用 5 Locale i18n (`apeireth_i18n` crate), `Organ::name(tr)` async 翻译.

**借鉴动作**: 0 (已用 R21 G-1, 不重复造轮子).

### 3.4 模式 4: widget 组件化 (不适用, 跨语言)

OpenCode 公开 doc 描述: SolidJS 组件 + state subscribe.

**当前 9 organ**: Rust + ratatui, 不是 SolidJS. 0 借鉴.

---

## 4. PHL-07 NotUnoptimizable 集成 (per 13 键 PHL-07 spec)

PHL-07 语义 = "代码不假装已优化". 9 organ 集成 PHL-07 = 禁止以下模式:

| 0 假装模式 | 当前是否存在 | PHL-07 强制要求 |
|------------|--------------|------------------|
| 缓存但 0 命中率 | ✅ 0 | 0 假装缓存 (let _ = cache 之类) |
| 锁但 0 持锁时间差 | ✅ 0 | 0 假装锁 (Mutex<()>::lock().unwrap() 之类) |
| async 但 0 await | ✅ 0 | 0 假装 async (.await 0 调用) |
| 指标但 0 报告 | ✅ 0 | 0 假装指标 (counter.fetch_add 0 调用) |
| 订阅但 0 触发 | ✅ 0 | 0 假装订阅 (state.subscribe 0 callback) |

**借鉴 OpenCode 验证**: 9 organ snapshot 模式 1:1 借鉴, snapshot 实际读 atomics → 0 假装 = PHL-07 通过.

**实施位置**: `crates/apeireth-core/src/lib.rs` 的 `PhilosophyKey` enum 新增 `NotUnoptimizable` variant, 13 键编译期 hardcode 升级 (详见 `crates/apeireth-core/src/.r125-12-PHL-07-SPEC.md`).

---

## 5. R125-12 限流结束 0 装 src 实施计划 (5 阶段)

### 阶段 1: clone verify (限流结束 first thing)
- 真实 `git clone https://github.com/anomalyco/opencode '.openclaw\workspace\borrowed-repos\opencode\'`
- 验证 7a4b9c2 commit hash
- 列出 `packages/opencode/src/cli/cmd/tui/component/*` 9 organ widget 文件 (per 借鉴 ID 索引 §3)
- 0 装 = 真实读文件, 0 假装 clone 完

### 阶段 2: 内部 fn 借鉴 (per 借鉴 ID 索引 §5)
- 抽 `crates/apeireth-tui/src/organ/common.rs` (新增 module, 不动 9 文件名):
  - `pub fn humanize_ms(ms: u64) -> String` (heart.rs `age_phrase` 通用化)
  - `pub fn humanize_token(n: u64) -> String` (brain.rs / hand.rs 通用化)
  - `pub fn format_kv_line(k: &str, v: impl Display) -> String` (9 organ 通用)
- 9 organ 内部 fn 改 import 引用 common.rs
- 减量 -5.7KB (per 借鉴 ID 索引 §5 表)

### 阶段 3: 13 键 PHL-07 实施 (per PHL-07 spec)
- 修改 `crates/apeireth-core/src/lib.rs`:
  - `enum PhilosophyKey` 新增 `NotUnoptimizable` variant
  - `ALL_TWELVE_KEYS` 改 `ALL_THIRTEEN_KEYS` (新增 1 元素)
  - `TWELVE_KEYS_HARDCODE` 改 `THIRTEEN_KEYS_HARDCODE` (新增 group 7 = 1 元素)
  - group_id 7 = NotUnoptimizable
- 修改 `crates/apeireth-core/tests/verdict_keys.rs`:
  - 新增 5 单元测试: PHL-07 hardcode + group_id 7 + violation + 5 假装模式拒绝
  - 0 装: 测试真实跑 (per master 17:22 "cloned = real implementation")
- 修改 `crates/apeireth-tui/src/organ/.r125-12-13-keys-stub.rs`:
  - 9 organ 内部加 1 行 PHL-07 verify (snapshot 真实读 atomics = 0 假装)

### 阶段 4: 6 重守门 v6 verify (per B4)
- compile: `cargo check -p apeireth-tui -p apeireth-core` 0 error
- test: `cargo test -p apeireth-tui --lib` 0 fail + `cargo test -p apeireth-core --test verdict_keys` 19+5=24 pass
- lint: `cargo clippy -p apeireth-tui -p apeireth-core --all-targets` 0 warning
- LOC: 减量 5.7KB verified (per 借鉴 ID 索引 §5)
- doc: 8 硬墙 verify 100%
- behavior: 9 organ snapshot 模式 + 13 键 PHL-07 hardcode verify

### 阶段 5: final 报告 update
- 更新 `reports/agent-r125-12-final-2026-08-10.md`:
  - "0 装准备" → "0 装实施" 状态
  - 借鉴 ID 7a4b9c2 commit verify (0 装)
  - 9 organ 减量数字 verify (真实测量)
  - 13 键 PHL-07 编译期 hardcode verify (cargo test 24 pass)
  - 0 装 cargo check / cargo test / cargo clippy 输出
- 0 主动 commit (待 master 集成时统一 commit)
- 0 主动 push (待 master 集成时统一 push)

---

## 6. 8 硬墙 verify (per B7 锁, 限流前预 verify, 限流后真 verify)

| 硬墙 | R125-12 守住方式 | 限流前 verify | 限流后 verify |
|------|------------------|----------------|----------------|
| 1. workspace.version 1.2.0 0 改 | 不改 workspace Cargo.toml | ✅ 0 改 | ⏳ 限流后 |
| 2. R11 baseline 3 值数字严守 | V1141/V1131/V1136 0 改 | ✅ 0 改 | ⏳ 限流后 |
| 3. 24 LOCKED 持续更新 + 9 organ 文件名 + 入口签名 0 改 | 仅改 9 文件内部 fn | ✅ 9 文件名 + 9 入口签名 0 改 | ⏳ 限流后 |
| 4. 6 → 8 哲学锚 (B5) | 不动 6 哲学锚 | ✅ 0 改 | ⏳ 限流后 |
| 5. V0.5 25 维 → 30 维 (B3) | 不动 V0.5 维 | ✅ 0 改 | ⏳ 限流后 |
| 6. 6 重守门 v6 (B4) | 6 重 verify (阶段 4) | ✅ 计划完整 | ⏳ 限流后真跑 |
| 7. 13 键 (A3: 12 + PHL-07, R125-12 实施) | 新增 PHL-07 (PHL-07-SPEC) | ✅ spec 写完 | ⏳ 限流后编译期 hardcode |
| 8. C1 0 commit + C2 0 装解除 + C3 升 6 重 v6 + 0 push | 0 主动 commit + 0 主动 push | ✅ 0 commit + 0 push | ⏳ 限流后保留 0 主动 commit |

---

## 7. R125-12 任务边界 (per master 17:22 决策)

| 边界 | R125-12 行为 |
|------|--------------|
| 借鉴源码 0 clone | ⏳ 限流 = 准备, 写本 plan + 索引 + stub |
| 借鉴源码 0 装 = 真实施 | 限流结束补 0 装 src (待办 per §5) |
| 8 硬墙 (master 17:22 升级授权) | 100% 守住 (per §6) |
| 0 主动 commit | ✅ 0 commit (本 plan 文件 untracked) |
| 0 主动 push | ✅ 0 push (master HEAD = 059dd30) |
| 8/20 截止 | 当前 8/10, 距 8/20 还有 10 天 (充分) |
| 减量 199 → 120KB (-40%) | 当前 109.6KB < 120KB, 已达标 (§1) |

---

_R125-12 sub-agent · Mavis 派 · P2 路线 · 2026-08-10 · 0 装准备 + 0 装 · 借鉴 ID R124-1-BORROW-anomalyco/opencode-7a4b9c2-2026-08-10_
