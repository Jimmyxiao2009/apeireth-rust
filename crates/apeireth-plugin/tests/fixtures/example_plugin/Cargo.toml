[package]
name = "apeireth-plugin-example"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
authors = ["Apeireth Team"]
license = "Apache-2.0"
description = "Example plugin fixture for apeireth-plugin in-process test (Fixture 5, per v09021-rust-translation-blueprint-RIVAL §4.1)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "apeireth_plugin_example"
path = "src/lib.rs"
