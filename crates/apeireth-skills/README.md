# apeireth-skills

> apeireth-skills — 可复用能力声明: id/version/input_schema/output_schema + Registry + 严格 semver 3 段比较 (R23 P1 #5 实质化, lib.rs:94). 10 pub 模块 (descriptor / anthropic_skills / eval_bridge / file_loader / library_stage6_guardianship / mcp_bridge / semver_strict / skill_executor / wasm_bridge / watcher) + 1 private (organ_kani_proofs). TP23 加双通道 SkillRegistry (Capability + Discipline 互斥). **0 改 workspace.version**. 旧 README "R23 6 module (cron/skills/acp/config/test/eval)" 系 R23 设计稿残影, 实际 src 是 10 模块 (代码 `src/lib.rs:18-29`).

apeireth-skills 是 Apeireth 1.0 (AGI 操作系统) 工作区 crate 之一。完整架构见 [docs/](../../docs/README.md)。
