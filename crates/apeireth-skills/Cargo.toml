[package]
name = "apeireth-skills"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "apeireth-skills — R23 6 module (cron/skills/acp/config/test/eval) skills 子模块: 可复用能力声明 + 描述 + 输入/输出 schema + 版本"
[lib]
name = "apeireth_skills"
path = "src/lib.rs"
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
parking_lot = "0.12"
thiserror = { workspace = true }
walkdir = "2.5"
apeireth-mcp = { path = "../apeireth-mcp" }
[lints]
workspace = true
# R125-19 (per decision-51 §1.4 P3-2): 加 skill_executor_demo example
# 5 phase state machine 演示 (TDD / Plan-Verify / Parallel / Review / Meta).
# 借鉴 ID: R125-19-BORROW-obra/superpowers-2026-05-2026-08-10
[[example]]
name = "skill_executor_demo"
path = "examples/skill_executor_demo.rs"
