[package]
name = "apeireth-skills"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "apeireth-skills — 可复用能力声明: id/version/input_schema/output_schema + Registry + 严格 semver 3 段比较 (R23 P1 #5 实质化). 10 pub 模块 + 1 private (organ_kani_proofs). TP23 加双通道 SkillRegistry (Capability + Discipline 互斥). 0 改 workspace.version."
[lib]
name = "apeireth_skills"
path = "src/lib.rs"
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
parking_lot = "0.12"
thiserror = { workspace = true }
walkdir = "2.5"
apeireth-mcp = { path = "../apeireth-mcp" }
# R174 stage 6: WASM skill bridge (WasmSkillExecutor + WasmSkillDescriptor)
apeireth-sovereignty = { path = "../apeireth-sovereignty" }
[lints]
workspace = true
# R125-19 (per decision-51 §1.4 P3-2): 加 skill_executor_demo example
# 5 phase state machine 演示 (TDD / Plan-Verify / Parallel / Review / Meta).
# 借鉴 ID: R125-19-BORROW-obra/superpowers-2026-05-2026-08-10
[[example]]
name = "skill_executor_demo"
path = "examples/skill_executor_demo.rs"
