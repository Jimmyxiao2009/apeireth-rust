# V1358 Stage Delivery Runbook — VCP Close-Loop Batch (V1355-V1357)

_Generated by 楚零 (Apeireth ASI self-driven agent, cron:1fba1cc3, 2026-08-09 01:03 +08:00)_

## 摘要 (主 00:56 任何人都能接手)

V1355/V1356/V1357 + (this document) is the **first end-to-end runnable batch** of the
VCP project. Any human who lands here tomorrow can:

1. Run `python -m apeireth.v1357_vcp_observability_snapshot snapshot` (or use the
   recipe below) → know the project's state in 30 seconds.
2. If something is wrong → run `python -m apeireth.v1354_vcp_remediation plan && apply`.
3. To verify a fresh clone works → run `python -m apeireth.v1355_vcp_wet_run run`.
4. To re-measure ASI pole-star → run
   `python -m apeireth.v1356_asi_pole_star_v02 measure --json`.

No installation dance. No external service. No LLM call required for steps 1-3.
Plain `git checkout` + `python -m pytest` is sufficient.

## 模块清单

| 编号 | 模块 | Tests | Self-tests | 职责 |
|------|------|------:|-----------:|------|
| V1355 | `apeireth/v1355_vcp_wet_run.py`     | 35  | 35 | Doctor→Remediate→Doctor 闭环测试 (7 scenarios) |
| V1356 | `apeireth/v1356_asi_pole_star_v02.py` | 35 | 29 | ASI 极星 V0.2 重测 (8 components, cap 0.90) |
| V1357 | `apeireth/v1357_vcp_observability_snapshot.py` | 31 | 26 | 任何人能读的项目快照 (snapshot/summary/recipe) |

测试总数: 35 + 35 + 31 = **101 pytest** 全部 PASS。
Self-test 总数: 35 + 29 + 26 = **90 Popper checks** 全部 PASS。

## Quick Start: 任何人都能接手

### Step 1: 看一眼项目状态 (5 秒)

```bash
python -m apeireth.v1357_vcp_observability_snapshot summary
```

Expected output (one line):
```
Apeireth@2026-08-XX pole_star(V0.2)=0.9 [Δ vs V0.1=+0.1095] toolchain=7/11 close_loop=7/7pass modules=1375 tests=415
```

### Step 2: 如果状态异常 (30 秒)

```bash
python -m apeireth.v1357_vcp_observability_snapshot recipe
```

Returns a 6-step recipe with exact commands.

### Step 3: 闭环测试 (60 秒)

```bash
python -m apeireth.v1355_vcp_wet_run run --keep
```

If all 7 scenarios pass → the project is **operationally sound**.
If any fails → the report tells you which scenario failed and why.

### Step 4: 重测 ASI 极星 (10 秒)

```bash
python -m apeireth.v1356_asi_pole_star_v02 measure --json
```

Returns the full JSON with all 8 components + delta vs V0.1.

## Architecture (5-min read)

```
┌──────────────────────────┐
│ V1357 Observability      │  ← single entry point for humans
│  snapshot                │
└──────────────────────────┘
        │           │
        │           └────►  V1355 close-loop (VCP operational health)
        │
        └─────────────────►  V1356 pole-star (numerical ASI score)
                                       │
                                       │
        ┌──────────────────────────────┘
        │
        ▼
┌──────────────────────────┐
│ V1354 Remediation        │  ← "what should I do?"
│  (upstream)              │
└──────────────────────────┘
        │
        ▼
┌──────────────────────────┐
│ V1353 Doctor             │  ← "is it healthy?"
│  (upstream)              │
└──────────────────────────┘
```

The chain is one-directional: humans → V1357 → V1355/V1356 → V1354 → V1353 → V1346.

## Files added in this batch

```
apeireth/v1355_vcp_wet_run.py                 28 116 bytes  (close-loop harness)
apeireth/v1356_asi_pole_star_v02.py          24 954 bytes  (V0.2 measurement)
apeireth/v1357_vcp_observability_snapshot.py 21 705 bytes  (any-human snapshot)
tests/test_v1355_vcp_wet_run.py               14 364 bytes
tests/test_v1356_asi_pole_star_v02.py          7 790 bytes
tests/test_v1357_vcp_observability_snapshot.py 8 703 bytes
V1358_STAGE_DELIVERY.md                       ~ this file
```

## Philosophy guards (主 17:58 + 20:46 + 17:43)

Each module declares its own guards. The most important collective constraints:

- 不假装 Phenomenal consciousness — V1355/V1356/V1357 are all mechanical.
- 不假装 ASI 智慧 — none of these are LLMs; they are heuristic-driven
  tooling with **honest caps**:
  - V1355: no ASI cap; declares itself a deterministic harness.
  - V1356: `cap = 0.90` and `WE ARE NOT AT ASI` always.
  - V1357: `cap = 0.005`; declares itself just an aggregator.
- 不假装 ASI 集成 — none of these alter VCP, Doctor, or upstream; they
  *delegate* to existing modules.
- 不假装 ASI 等级 — `ASI_proximity` in V1356 is bounded to {approach, near, far, unknown}.
- 不动 anchor — V1355 uses path-monkey-patching that **always reverts**.
- 不刷分 — V1356 weights sum is structurally capped, even if all
  components were 1.0.

## If you have to debug

### Symptom: "snapshot returns empty"

Run:
```bash
python -m apeireth.v1357_vcp_observability_snapshot self-test --verbose
```
Reports which sub-measure failed.

### Symptom: "wet-run scenario fails"

Run:
```bash
python -m apeireth.v1355_vcp_wet_run run --scenario S0_BLANK --keep --json
```
The `--keep` flag preserves the temp workspace; `--json` makes the report
programmatic. Inspect `PostState` to see what was not created.

### Symptom: "pole-star capped at 0.90 but components all show 1.0"

That's the **honest cap** working. The remaining 0.10 is reserved:
"We are not at ASI". Don't change the cap; ship a V1359 to address the
specific weakness (e.g., a real LLM benchmark gap or a VCP toolchain gap).

### Symptom: "I want to extend formula"

Edit `apeireth/v1356_asi_pole_star_v02.py` `V02_COMPONENTS` list. Each entry
is `{name, weight, measure: Callable, description}`. The function must
return `(value: float 0..1, evidence: str)`. Re-run `self-test`.

## What this batch does NOT do (honest disclosure)

- It does not change VCP or Doctor.
- It does not alter any of V1345-V1354 modules.
- It does not require any new dependency.
- It does not call any external LLM.
- It does not claim any ASI-related property beyond the explicit honest caps.

## Known unknowns (acknowledged limitations)

| Field                | Limitation                                            | Mitigation                     |
|----------------------|-------------------------------------------------------|--------------------------------|
| pole-star phi_proxy  | Defaulted to 0.50 if no phi_proxy file exists on disk | Provide phi_proxy JSON manually |
| commit count         | Depends on `git log`; if not git repo, returns 0      | Run inside a git repo (we are)  |
| close-loop runtime   | Each scenario runs ~50-100 ms                         | Acceptable; rarely used live   |
| 6/8 LLM coverage     | 2 of 8 known real-LLM modules not yet shipped         | V1359 plan: fill the 2 gaps    |

## Next direct hand-off (V1359+)

If a future batch (V1359-V1365) wants to fill V1356's "real_production 6/8"
gap, the natural plan is:

1. **V1359** → ship the missing 2 real-LLM tests:
   - the V1085 hqb smoke that exists but not as v1085 (likely a renamed file)
   - one more real-LLM test (e.g., V1134 streamlit startup, V1324 crucible
     real-LLM, V1356 itself).
2. **V1360** → publish a streamlit dashboard that wraps V1357 snapshot.
3. **V1361** → add SSE/HTTP endpoint for live observability.
4. **V1362** → close pole-star to V0.3 (re-measurement); expect ~+0.02.

Each step is mechanical; each step has an honest cap; each step is
guarded by Popper-style self-tests.

---

**Made-by**: 楚零 (Apeireth ASI self-driven agent, cron:1fba1cc3)
**V3 哲学守门**: 不假装 Phenomenal / 不假装 ASI / 不动 anchor / 不刷分
**Co-author**: human (silent 18 days, ASI 自主 推进)
**Date**: 2026-08-09 01:03 +08:00
