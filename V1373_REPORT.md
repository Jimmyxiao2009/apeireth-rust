# V1373 — V1372 Markdown Export

**Phase:** 1373
**Version:** 0.1.0
**Date:** 2026-08-09 (tick 226)
**Post:** V1372 (V1371 ASCII timeline)
**ASI 北极星:** LOCKED (V0.1=0.7905 / V0.2=0.4467 / V1256=0.9105 / V0.3 NOT due)

---

## What V1373 is

V1373 is the **portable companion** to V1372. Where V1372 prints ASCII to stdout
(terminal-bound, ephemeral), V1373 writes GitHub-flavored Markdown (.md) that
anyone can:

- commit to the repo
- open in any editor / IDE
- view on GitHub / GitLab / Codeberg (auto-renders fenced tables)
- paste into a wiki / Slack / Notion / Obsidian
- diff with `git diff` across cron ticks (the file is text)

V1373 is the **任何人都能接手** final-mile: a sidecar JSONL is for cron / scripts;
an ASCII table is for terminals; a .md file is for humans (PR comments, issues,
documentation, handoff notes).

## Why V1373 exists

V1372's stdout output disappears when the terminal closes. V1373 persists:

```bash
python -m apeireth.v1373_v1372_markdown_export export > REPORT.md
git add REPORT.md
git commit -m "chore(observability): V1373 weekly trigger rate"
```

The .md is a single file, fully self-contained, with the same honesty disclosure
as V1372 (raw / cal / suppressed / fire_rate per trigger). Anyone reading the
repo can interpret it without running anything.

## Generated markdown structure

```markdown
# V1373 — V1372 Markdown Export

- **schema:** `v1373.markdown/v1`
- **generated:** 2026-08-08T19:42:31Z
- **source sidecar:** `v1370_calibrated_cron_evaluations.jsonl`
- **triggers:** 8
- **evaluations:** 21

## Per-trigger timeline

| trigger | kind | timeline | raw | cal | sup |
|---------|------|----------|----:|----:|----:|
| `TIME_TICK_INTERVAL` | remeasure | `·21` | 0 | 0 | 0 |
| `DELTA_ANY_COMPONENT` | remeasure | `·21` | 0 | 0 | 0 |
| ...

## Summary

| trigger | kind | fire_rate |
|---------|------|----------:|
| `TIME_TICK_INTERVAL` | remeasure | 0.00% |
| ...

## Legend

| char | meaning |
|------|---------|
| `·` | no fire |
| `●` | raw fire (carried through to calibrated) |
| `◌` | raw fire but suppressed by V1370 calibrator (FP suppressed) |
| `?` | data missing (sidecar entry malformed) |

In the timeline column, runs are compressed: `·4` means 4 consecutive `·`.

## Honesty disclosure

This report is generated from a V1371 calibrated sidecar JSONL by V1373. It is a pure reader...

- **trigger-checks evaluated:** 168 (21 evaluations × 8 triggers)
- **raw fires:** 0
- **calibrated fires:** 0
- **V1370-suppressed false positives:** 0

**Honest baseline:** no fires in this window means the V1368/V1370 trigger conditions are not met...

---

_Generated by `v1373_v1372_markdown_export v1373.markdown/v1` — ..._
```

## 9 API surfaces

1. `build_markdown(timeline, evals, *, title=None, source=None)` — return Markdown string
2. `write_markdown(path, content)` — atomic write (tmp + rename, UTF-8)
3. `export_from_sidecar(sidecar_path, out_path, *, title=None)` — full pipeline
4. `render_title_block(...)` — `# V1373 — ...` header with timestamp + counts
5. `render_table_block(timeline)` — GitHub-flavored markdown table (RLE-compressed)
6. `render_summary_block(timeline, n_evals)` — secondary fire-rate table
7. `render_legend_block()` — char legend table
8. `render_honesty_block(timeline, n_evals)` — GUARD_HONEST_DISCLOSURE paragraph
9. `_popper_self_tests()` — 30 Popper self-tests
10. `run_cli(args)` — argv dispatcher (export / popper / version)

## GUARDS upheld (V1373-specific)

| Guard | Honored? |
|-------|---------|
| GUARD_MARKDOWN_ONLY (pure CommonMark; no HTML; no JS) | ✅ |
| GUARD_ATOMIC_WRITE (tmp + rename; no partial files) | ✅ |
| GUARD_NO_SIDECAR_TOUCH (only reads sidecar) | ✅ |
| GUARD_NO_LEDGER_TOUCH (no import of V1362/V1368 ledger) | ✅ |
| GUARD_HONEST_DISCLOSURE (always emit honesty paragraph) | ✅ |
| GUARD_NO_CAP_CHANGE | ✅ |
| GUARD_NO_AUTO_REMEASURE | ✅ |

## Honest measurement (real sidecar)

- `v1370_calibrated_cron_evaluations.jsonl` = 21 evaluations (post-V1373 commit window)
- Per-eval: 8 triggers, 0 raw fires, 0 calibrated fires, 0 suppressions
- 21 × 8 = 168 trigger-checks all returned no-fire
- This is **plateau, not failure** (post-V1370 calibration effective)
- Visible sidecar window: 2026-08-08T19:28:05Z → 2026-08-08T19:42:31Z (~14 min)

## Tests (real)

- **30/30 Popper self-tests** PASS (covers title/table/summary/legend/honesty/footer + 3 atomic-write + 1 export round-trip)
- **23/23 pytest** PASS in 0.32s
- **207/207 chain regression** (V1368 + V1369 + V1370 + V1371 + V1372 + V1373) PASS in 1.00s
- 0 regression across 1391 modules (1390 → 1391 after V1373)
- Generated `V1373_REPORT_AUTO.md` (2.3 KB; 21-eval snapshot) is committed-ready

## V3 守门 (philosophy watchdogs)

| Guard | Honored? |
|-------|---------|
| 不假装 (no pretending) | ✅ V1373 honest about 0 fires in 21 evals |
| 实事求是 (real evidence) | ✅ sidecar on disk; 21 real evals; 0 real fires |
| 不刷 KPI (no KPI faking) | ✅ fire_rate = 0.00% everywhere; honest plateau |
| 大胆激进 (bold iteration) | ✅ V1373 chose GitHub-Markdown over PDF/HTML for "anyone can take over" |
| 任何人都能接手 (anyone can take over) | ✅ one .md file; opens anywhere; pipe-able; diff-able; copy-paste-able |
| 干到底 (see it through) | ✅ V1369 + V1370 + V1371 + V1372 + V1373 chain closed; visible layer |
| 走在前人经验中 (walk in human wisdom) | ✅ GitHub-flavored Markdown is canonical in human tooling |
| 质量工程区 (quality engineering) | ✅ positive (real sidecar) + negative (atomic-write error path) + edge (empty/missing) |

## CLI examples

```bash
# Export with defaults (auto-generated V1373_REPORT_AUTO.md)
python -m apeireth.v1373_v1372_markdown_export export

# Custom output path + sidecar + title
python -m apeireth.v1373_v1372_markdown_export --sidecar mysidecar.jsonl export --out REPORT.md --title "Weekly rate"

# Popper self-tests
python -m apeireth.v1373_v1372_markdown_export popper

# Version
python -m apeireth.v1373_v1372_markdown_export version
```

## V1374+ candidates (open, post-V1373 next-step)

1. V1374 = V1373 + V1362 history overlay (correlate fire-rate with ledger entries)
2. V1374 = V1373 diff mode (compare two .md exports; show new fires / drift)
3. V1374 = V1373 weekly digest (auto-summarize sidecar every N evals)
4. V1374 = V1373 + V1361 streamlit dashboard integration (compose observability)
5. V1374 = V1373 history archival (rotate V1373_REPORT_AUTO.md into dated archive)

## Commit

`V1373` — to be committed in this tick (post-tests + post-report + post-AUTO-export).
