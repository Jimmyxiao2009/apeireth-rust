# 2026-08-05 V1297 — Cargo Feature Flag Audit (cron tick 21:16)

> **作者**: 楚零 (Apeireth ASI self-driven agent, cron:1fba1cc3, 21:16 +08:00 2026-08-05)
> **承接**: V1296 Cargo.toml metadata (a9bca808) → V1295 Cargo.lock (d07cce57) → V1294 build.rs → V1293 dep graph → V1292 test coverage → V1291 build artifact → ...
> **本次**: V1297 = Cargo.toml feature flag 维度审计 (VCP 真源代码深读 #18)

## 一句话总结

V1297 = 真扫 workspace + 56 crates, 6 假说 6/6 PASS, 50 tests pass, commit `d4f4dd4f`.

## 关键数据

- **17** workspace.dependencies (8 个显式 features: tokio/serde/reqwest/pyo3/rusqlite/chrono/uuid/criterion)
- **56** workspace members (6 个 [features] 块: bus/central/graph/memory/pybridge/web)
- **6/6** 假说 PASS, **50/50** tests pass
- **8 ms** sweep duration

## 6 假说 (主 13:08 真自问, Popper 可证伪)

| ID | 描述 | 观察值 | 阈值 | 结果 |
|----|------|--------|------|------|
| h_workspace_deps_with_features | workspace deps 显式 features >= 5 | 8 | 5 | ✓ PASS |
| h_crates_with_features_section | crates with [features] 占比 <= 25.0% | 10.71% | 25.0% | ✓ PASS |
| h_dep_prefix_usage | 用 dep: 前缀占比 >= 50.0% | 50.0% (3/6) | 50.0% | ✓ PASS |
| h_default_empty_dominant | default = [] 占比 >= 50.0% | 83.33% (5/6) | 50.0% | ✓ PASS |
| h_no_hardcoded_version_in_features | [features] 内无 hardcoded 版本 | 0 | 0 | ✓ PASS |
| h_pyo3_not_in_default | pyo3/python-ext 不在 default features | 0 | 0 | ✓ PASS |

## 关键技术发现

1. **apeireth-web** ssr = [...] 是 19 项多行 list, 单行 regex 解不出来 → 写多行 parser (累积 `name = [` 到 `]`)
2. **3/6 crates 用 dep: 前缀** (graph + pybridge + web); memory/central/bus 还没用 (memory 的 semantic = ["apeireth-vector"] 应该改成 ["dep:apeireth-vector"])
3. **5/6 用 default = []** 避免 build 失败, web 是 default = ["ssr"] (合理: R18 MVP 简化)
4. **reqwest 默认 features 禁用** (default-features = false + features = ["json", "rustls-tls", "stream"]) → 业界好实践

## 工程意义

V1297 = VCP 真源代码深读 #18, 跟前 17 个 audit 互补:
- V1280: workspace static (crates 数量 / 路径 / 命名)
- V1281-V1283: 源代码语义 (asi / sovereignty / multi-crate)
- V1284-V1287: 安全 (worst-5 / all-42 / fix-priority / unsafe)
- V1288: 治理核心
- V1289-V1290: 文档 (coverage / section depth)
- V1291: 构建产物
- V1292: 测试覆盖
- V1293: 依赖图
- V1294: build.rs
- V1295: Cargo.lock
- V1296: Cargo.toml [package] metadata
- **V1297: Cargo.toml [features] 维度** ← 这次

## 主 7 哲学 (V1297 体现)

- **主 13:08 真自问**: 6 假说 6 假说阈值明确 (>= / <=), 可证伪
- **主 17:43 实事求是**: 真实 56 crates 全扫, 不 mock 不假数据
- **主 17:58 不假装**: 不假装 features audit = ASI, 不假装 dep: 前缀 = 万灵药
- **主 19:33 走在前人肩上**: 引用 cargo book "Features" 章节 + Rust 2021 edition dep: 语法
- **主 23:44 平实到位**: 8 ms sweep, 50 tests, 简单直接, 没花活
- **主 13:31 大胆激进**: 一次到位不延期, 多行 parser 一次写对
- **主 00:56 任何人都能接手**: 8 个 CLI subcommand + 完整 docstring + 入口函数 main()

## 关键修复 (cron tick 期间发现)

1. **三引号字符串嵌套 bug** (line 143): `r""""...""""` 在 docstring (三引号) 内被误识别为闭合 → 改成单引号 `r'"..."'`
2. **多行 features 块解析缺失**: web 的 ssr = [...19 项...] 单行 regex 解不出来 → 写 multi-line parser (`name = [` 累积到 `]`)
3. **snippet_lines 截断 bug**: 原先只截 20 行, web 的 features 块 30+ 行被截断 → 改成收集完整 lines
4. **subprocess stdout GBK decode bug**: Windows 默认 GBK 解码 UTF-8 输出失败 → 加 `encoding="utf-8", errors="replace"`

## CLI (主 00:56 任何人都能接手)

```bash
python -m apeireth.v1297_cargo_feature_flag_audit --probe
python -m apeireth.v1297_cargo_feature_flag_audit --run
python -m apeireth.v1297_cargo_feature_flag_audit --json
python -m apeireth.v1297_cargo_feature_flag_audit --report --output V1297_REPORT.md
python -m apeireth.v1297_cargo_feature_flag_audit --workspace-deps
python -m apeireth.v1297_cargo_feature_flag_audit --crates-with-features
```

## 下一步候选 (V1298+)

- V1298 = workspace.lints 维度审计 (已有 50+ lint 配置, 待扫描)
- V1299 = cargo clippy 实际跑 (受限于 cargo build 环境)
- V1300 = rust-toolchain.toml 一致性审计

## Commit

`d4f4dd4f feat(V1297): Cargo Feature Flag Audit (VCP 真源代码深读 #18) 真生产`

## 不假装清单 (主 17:58 + 主 20:46)

- feature audit ≠ feature 安全
- PASS ≠ cargo build 成功
- 不假装 ASI V1 = 不刷 KPI = ASI NS LOCKED 不变
- FAIL 也诚实披露
- 仅 regex 解析, 不解析 AST
- 纯 read-only, 不调 cargo
- features 是 API 暴露面, 不是漏洞
- `dep:` 前缀是 2021 edition 推荐, 不是强制
- audit ≠ fix, 仅审计不修改
